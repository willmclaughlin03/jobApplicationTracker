# Fixes

Use this file to briefly record fixes when preparing a push to a pull-request branch so the entry is included in that push. Do not update it for individual edits, intermediate commits, or local-only changes.

## What to record

- Summarize only fixes included in the PR push being prepared.
- Briefly describe the issue that was addressed.
- Note the approach taken to fix it.
- Summarize how the fix was implemented.
- Add the date so the fix history stays readable.

## Entry Template
- `YYYY-MM-DD` - `Issue name`: issue summary. Approach: short note on the strategy used. Fix: short note on how it was resolved.

## Entries

- `2026-09-06` - `Artifact acceptance exceeded Next request-path support`: Matching manifest regexes accepted empty, whitespace-only, and other build IDs that Next request handling could not consume. Approach: validate a nonempty unchanged URL segment before checking manifests, and verify retained version-ID behavior over real HTTP. Fix: reject split, encoded, and URL-altered IDs, correct success/rejection fixtures, and add an owned temporary build/start regression to the existing runtime script. All 100 focused tests, targeted ESLint, diff checks, and the Next 16.3.3 Webpack HTTP fixture pass; full application and deployed qualification were not rerun.

- `2026-09-06` - `SSR suffix wildcard and empty build-ID URL mismatch`: Unescaped suffix dots passed the build guard, and concatenated URLs rejected Next-generated empty-ID routes by adding a duplicate slash. Approach: add a suffix near-match probe and match Next's POSIX URL joining in the expected URL and related probes. Fix: escape the default fixture suffixes, reject replacement-dot and off-route patterns for empty and non-empty IDs, and cover `buildDataRoute('/', '')`. All 63 artifact tests, targeted ESLint, and diff checks pass; full production build and deployed qualification were not rerun.

- `2026-09-06` - `Custom build IDs rejected and broad SSR patterns accepted`: Character restrictions rejected valid version IDs, while positive-only URL checks accepted patterns spanning other builds or routes. Approach: retain string-type validation and require negative URL probes that respect dynamic and catch-all routes. Fix: remove both build-ID character checks, reject incorrect-build and off-route matches, replace the broad build fixture with Next-generated patterns, and document uncovered negative-test callbacks. The two focused suites pass 73 tests; targeted ESLint and diff checks pass. Full production build and deployed qualification were not rerun.

- `2026-09-05` - `Artifact checker bypassed build-ID validation`: Direct calls to `checkProtectedPageArtifacts` could accept missing or invalid IDs when data-route patterns matched because only the filesystem wrapper validated the ID. Approach: validate at the exported artifact boundary before route comparisons. Fix: require a nonempty string matching the established build-ID format and retain the existing error message; add missing, malformed, non-string, and valid-ID cases and assert the existing mismatch diagnostic. All 32 focused tests, targeted ESLint, and diff checks pass.

- `2026-09-05` - `Non-matching protected-page data-route regexes`: Anchored, compilable patterns such as `^$` passed the build guard despite matching no data URL. Approach: verify route semantics using the actual build ID and concrete dynamic parameters. Fix: test every protected route's regex against its normalized Pages Router data URL, cover invalid matches in the artifact tests, and expand the route-discovery helper JSDoc. The two focused suites pass 36 tests, targeted ESLint and diff checks pass, and the existing seven-route build artifacts pass; a fresh build and deployed qualification were not run.

- `2026-09-05` - `Runtime harness output retention, cookie counts, and shutdown timeout`: The harness retained raw output in a private suffix, counted deletion cookies as writes, and discarded a child-process shutdown timeout. Approach: preserve only numeric streaming matcher state, report distinct cookie operation counts, and escalate only the owned process after timeout. Fix: added independent stdout/stderr match progress and observable flag tests, excluded deletions from writes and corrected the deletion-only qualification assertion, and sent SIGKILL before stream cleanup when the five-second wait expires. All 19 harness tests, targeted ESLint, and diff checks pass; full runtime/browser/deployed qualification was not rerun.

- `2026-09-05` - `Protected-page static cache HITs despite private response headers`: Signed-in documents for `/` and `/admin` returned Vercel cache HITs, violating the existing no-HIT qualification requirement without establishing personalized-data or cookie replay. Approach: prevent automatic static optimization across the entire protected-page inventory and own cache policy at both rendered-page and middleware response boundaries. Fix: added six explicit `getServerSideProps` exports and shared private/CDN no-store headers across those pages and existing admin SSR; strengthened extension-aware inventory reconciliation and added a CI production-manifest guard. Added real Next.js production HTTP coverage for document/data routes, refresh forwarding, cookie deletion and merging, and successful/denied admin responses.
  - Validation: all 118 unit suites / 1,775 tests, lint, production build, seven-route artifact checks, and 24 local production HTTP scenario groups pass. Infrastructure-free integration checks pass 28 tests with 192 remote-dependent tests skipped.
  - Boundary: partial CHUNK-6 cache correctness with some deployed GATE-1 evidence collection only. Browser/deployed no-HIT, cookie/session isolation, and successful-admin qualification remain outstanding; existing authentication and authorization behavior is unchanged. No full CHUNK-6 or GATE-1 completion is claimed.

- `2026-09-03` - `Hard-coded route-policy inventory samples`: The middleware inventory test duplicated page URLs separately from its protected and public file inventories, allowing a newly inventoried protected page outside an existing namespace to miss classifier coverage. Approach: derive representative URL paths from the authoritative file inventories while handling index and dynamic routes. Fix: mapped every protected and public page entry into a classifier assertion; the focused 6-test suite and changed-file ESLint pass.

- `2026-09-03` - `Auth-response public cache and middleware cookie propagation`: Protected and cookie-capable responses could retain Next.js's public revalidation policy on early wrapper exits, and refreshed middleware cookies could be lost when redirect creation replaced the response. Approach: apply the frozen `private, no-store` contract before protected API work, classify page routes before Supabase construction, collect final cookie descriptors by name, and apply them once to the selected protected response. Fix: hardened the shared wrapper and API adapter, explicitly covered sign-out and SSR cookie routes, corrected route-owned billing/storage headers, preserved real 404 handling for unmatched pages, and added route-level plus inventory regressions. This is a partial CHUNK-6 cache-correctness change and also gathers some deployed GATE-1 evidence during post-merge acceptance.
  - Validation: the 16 focused suites pass 311/311, all 113 unit suites pass 1,725/1,725 with no skips, repository lint and `git diff --check` pass, the explicit-fixture production build passes, and sanitized local production canaries pass the cache, route-boundary, and clean-shutdown checks.
  - Deployed evidence and remaining scope: cache-patch deployment attribution, the temporary-session-ceiling summary, signed-out hosted canaries, and the two-profile isolation matrix remain uncollected pending merge and deployment. This does not complete CHUNK-6, GATE-6, or GATE-1; D-D1, D-D2, and D-D4 provider-mode work plus WAF, load, rollback, v2 auth, and later sign-out correctness remain open.

- `2026-09-03` - `Vercel API sanitizer module-load failure`: `/api`, `/api/[id]`, and server-rendered sanitizer consumers could fail before application middleware because `isomorphic-dompurify@2.36.0` resolved `jsdom@28`, whose CommonJS dependency chain reached ESM-only `@exodus/bytes`. Approach: apply the upstream-recommended workaround only beneath `isomorphic-dompurify`, preserve Jest's independent DOM runtime, and cover both real sanitization and the strict-CommonJS loader boundary. Fix: pinned the scoped transitive `jsdom` resolution to `25.0.1`, regenerated the lockfile to remove the incompatible production path, and added a regression that fails on the former graph; the dependency tree, strict-CommonJS smoke, 130 focused tests, all 1,662 unit tests, lint, production build, local `/api` boot canaries, unchanged production-audit baseline, and diff checks pass, with deployed Vercel canaries still required.

- `2026-09-03` - `Undocumented auth-route canary callbacks`: Newly added middleware matcher and missing-session regression callbacks lacked the repository-required purpose and mock-connection comments. Approach: keep the follow-up comment-only and document setup, shared variables, module connections, and mock-state effects at each callback boundary. Fix: added concise block comments above the matcher `beforeAll`, both matcher assertions, and the missing-session test while preserving all test behavior.

- `2026-09-02` - `Signed-out CSRF and exact API-root routing failures`: The deployed signed-out CSRF canary returned retryable 503 because Supabase's expected `AuthSessionMissingError` status 400 fell into the unknown auth-outage default, while exact `/api` passed through the page middleware and redirected to login even though nested API routes were excluded. Approach: recognize only the SDK's named missing-session result as unauthenticated while preserving fail-closed handling for unknown and retryable errors, and close the matcher's exact-root boundary without excluding nearby page names. Fix: mapped `AuthSessionMissingError` to `AUTH_NOT_FOUND` so the existing failed-auth source limiter can return JSON 401, changed the matcher from `api/` to `api(?:/|$)`, and added classifier, middleware-response, exact-root, nested-route, and near-prefix regressions.

- `2026-09-02` - `Upstash auto-pipeline NOSCRIPT fallback`: The live Vercel canary validated its secret pair and acquired Redis but returned only retry-free 503 responses because `@upstash/redis` wrapped an uncached-script error as `Command failed: NOSCRIPT`, which the raw-only classifier rejected before its safe EVAL fallback. Approach: recognize only the SDK's exact anchored wrapper in addition to Redis's direct NOSCRIPT form so timeouts, transport failures, prefixed lookalikes, and ambiguous errors remain non-retryable. Fix: tightened the classifier to the two approved envelopes and added executor plus classification regressions proving exactly one fallback for valid forms and no replay for inexact forms.

- `2026-09-02` - `Rate-limit helper JSDoc gaps`: The shared legacy-source helper did not explicitly name the production canonicalizer and legacy serializer it reuses, while the limiter identifier classifier did not state that its bounded classification is side-effect free. Approach: keep the follow-up comment-only and preserve all existing parameter, return, and error annotations. Fix: documented the valid synthetic IPv4/IPv6 input and named production composition in `redisTestKey.js`, and clarified the bounded, deterministic, side-effect-free classifier contract in `rateLimit.js`.

- `2026-09-02` - `Rate-limit review follow-ups`: Limiter warnings lacked a safe error type, their tests depended on declaration-ordered clock offsets, a live Vercel integration reused one hourly source bucket across CI runs, and the legacy-source test helper could silently return `null`. Approach: preserve bounded identifier-free diagnostics and existing GET/read behavior while isolating each test run and making invalid fixtures fail immediately. Fix: added a 64-character constructor-name field without logging caught errors, advanced one shared warning clock before every diagnostic test, derived a canonical IPv6 source from the run-specific test user, and changed `legacySourceIdentifier` to return a string or throw with focused coverage.

- `2026-09-01` - `Rate-limit test cache isolation and fixture duplication`: The full-pipeline Vercel override restored environment and Redis state without clearing the memoized temporary-session HMAC/Redis pair, while two middleware suites duplicated the same canonical legacy-source expectation helper. Approach: reset both coupled runtime caches after restoring the environment and keep the production canonicalize/serialize composition in one Redis-focused test-support helper. Fix: added `temporarySessionSecrets.reset()` before Redis cleanup, exported one documented `legacySourceIdentifier`, and updated both suites to consume it.

- `2026-09-01` - `Vercel session composition and generic configuration attribution gaps`: The composed session-route suite mocked the shared ceiling, the production pipeline test exercised only a generic wrapper, and generic-only rate-limit functions never rotated the configuration event recorded by the deployment secret singleton. Approach: keep the source, secret, identity, ceiling, Redis credential, middleware, and route modules real in one deterministic production/Vercel route proof, and reuse the existing bounded telemetry summary cadence after generic loader access. Fix: added singleton/missing/duplicate-header and malformed-JSON route cases with cookie/Supabase short-circuit plus source/key/URL/token/identifier log sentinels, invoked shared telemetry rotation after generic Vercel configuration success or failure without affecting enforcement, and covered exactly-once success/failure summaries with bounded build/deployment attribution.

- `2026-09-01` - `CHUNK-1 Vercel provider and diagnostic gaps`: The completed temporary session ceiling still trusted AWS/CloudFront source metadata, loaded deploy-time credentials through AWS Secrets Manager refresh behavior, attributed telemetry to AWS, and the generic limiter logged raw provider errors plus arbitrary identifier prefixes. Approach: preserve the exact provider-neutral 400-request Redis/HMAC/Lua ceiling and wrapper ordering while replacing only deployment adapters, pinning the framework security release, and bounding every failure diagnostic. Fix: required explicit production Vercel source/secret modes with singleton raw-header proof, atomically memoized strict Vercel HMAC/Upstash values with no deployed fallback, shared the runtime pair with generic Redis consumers, removed AWS runtime dependencies, updated Vercel attribution and wrapper identities, and replaced raw limiter errors with throttled fixed-cardinality reason/classification fields plus sentinel regression coverage.

- `2026-08-31` - `CHUNK-0E operation-policy review gaps`: Provider-operation coverage did not directly exercise credential-bearing URL or unsupported-protocol rejection, request-policy tests did not consume their shared decision fixtures, and the tracker factory did not document its single mutable operation slot. Approach: keep the follow-up to focused regression coverage and an explicit concurrency contract while preserving reason-specific rejection assertions and runtime behavior. Fix: added both fail-closed URL cases, parameterized acceptance checks over the shared application-request fixtures, and warned that tracker instances must not be shared across concurrent Auth requests.
- `2026-08-31` - `Undocumented CHUNK-0E test callbacks`: Three review-added auth contract tests lacked the repository's short purpose documentation above their callbacks. Approach: keep the follow-up comment-only and describe each test at its behavior boundary. Fix: documented the accepted Node request shape, fail-closed Web `Headers` shape, and five-field provider-error logger boundary without changing implementation or assertions.
- `2026-08-31` - `CHUNK-0E logger isolation and request-shape ambiguity`: The provider-error unit test executed real Pino, while the request-intent policy described Node/Next candidates despite requiring Node's normalized and raw header representations. Approach: keep serialization integration coverage separate from the formatter unit contract and formally restrict the unused policy exports to their existing Node-style input model. Fix: replaced real Pino execution with an exact five-field logger-mock assertion, required plain or null-prototype header objects plus alternating-string `rawHeaders`, documented the adapter requirement for Web/Next `Headers`, and covered accepted Node and rejected Web request shapes.
- `2026-08-31` - `Gate-0 auth contracts lacked a production reuse boundary`: The frozen v2 schemas, SafeUser rules, exact Supabase tuples, and request-intent decisions existed only under `src/testSupport`, so later auth routes could not reuse them without importing test code or reimplementing security-sensitive behavior. Approach: mirror the approved contract into independent browser-safe shared modules and small server-only primitives while retaining fail-closed defaults, fixed-cardinality state, and no current-route integration. Fix: added strict production schemas/constants, three-field SafeUser normalization, per-request operation tracking, exact exported-class/code/status/operation classification, normalized-plus-raw header validation, bounded provider-error formatting, `X-App-Request` fixture reconciliation, and focused parity, permutation, duplicate-header, import-boundary, and Pino sentinel coverage.
- `2026-08-29` - `Stale temporary-session integration expectation`: The atomic integration suite still expected expired Redis buckets to make a valid active-window total fail closed, even after the production Lua and direct unit contract were corrected to normalize stale residue first. Approach: preserve the reviewed production behavior and align the higher-level integration assertion with the same active-window semantics. Fix: the regression now expects the request to be allowed and verifies that only the prior active request plus the current request remain stored.
- `2026-08-26` - `Silent Redis token validation failures`: Malformed Redis tokens returned `null` without recording why validation failed. Approach: classify token failures with fixed, credential-free reason codes after the existing URL validation branch. Fix: missing, non-string, empty, and over-2,048-character tokens now emit bounded diagnostics without token material, while URL errors retain their existing log and precedence; focused regressions cover every token category and the combined-invalid-credentials path.
- `2026-08-26` - `Temporary session stale totals and skip-path drift`: Redis hash counts from slots outside the active window were included in `stored_total`, so valid active state could fail closed, while public and protected invalid skip decisions duplicated the same log and 503 response. Approach: validate each stored slot first, normalize only stale positive slots before total enforcement and persistence, and route both skip decisions through one bounded helper. Fix: stale labels and counts now become the canonical empty pair before totals are accumulated, active totals above 400 still fail closed, cleanup and over-limit regressions execute the production Lua, and both middleware branches preserve the existing sanitized invalid-skip contract through `applyRateLimitSkipDecision`.
- `2026-08-24` - `Temporary session ceiling identity churn and decision consistency`: Alternating runtime-pair and generic-local Redis identities rebuilt a single shared client, telemetry hooks could replace allow/429/503 decisions with rejected promises, the post-client deadline branch read its clock twice, unknown source families with empty buffers could enter HMAC framing, and Lua policy literals could drift from exported constants. Approach: retain a bounded identity-keyed client set, route observability through one exception-safe boundary, snapshot the deadline once, validate the source family before its byte length, and derive script enforcement from the exported policy. Fix: added a two-entry LRU Redis client cache, protected record/rotation/finish telemetry hooks, reused one deadline result for reason and event selection, admitted only numeric family 4 or 6 with their existing byte lengths, interpolated Lua limit/window/slot/TTL values plus derived hash-field count, aligned parser bounds, and added focused regression coverage.
- `2026-08-24` - `Temporary session ceiling review follow-up gaps`: The Redis-script unit suite asserted only source structure, sparse-source snapshots hid per-key expiry evidence, and malformed protected-route skip callbacks lacked direct fail-closed coverage while caught errors were discarded before logging. Approach: execute the exact production Lua in memory, retain one normalized expiry entry per synthetic key, and preserve only bounded failure causes plus logger-standard caught errors internally. Fix: covered the 399/400 boundary, retry clamp, 61-slot reuse, and invalid stored totals; asserted every sparse key's expiry; added authenticated CSRF-protected malformed-skip coverage; and kept generic 503 responses free of callback error details.
- `2026-08-24` - `Temporary session ceiling review test gaps`: The v1 route lacked coverage for an asynchronously rejected shared-ceiling evaluation, while the atomic integration harness duplicated only the bucket math and could drift from the production Lua's Redis state checks. Approach: cover the middleware rejection boundary directly and execute the production script through an isolated in-memory Redis emulator instead of maintaining a second limiter algorithm. Fix: asserted the exact retry-free legacy 503 with no Redis quota or Supabase work, replaced the duplicated evaluator with the production Lua contract, and added invalid key type, hash shape/version, TTL, canonical numeric value, and stored-total regressions.
- `2026-08-23` - `Temporary session ceiling fleet and quota correctness`: The process-local session guard multiplied its 400-request allowance across instances and restarts, while the legacy generic AUTH limiter could reject legitimate session polling before the documented shared ceiling. Approach: move the authoritative decision into one Redis key per canonical HMAC-derived source, use Redis server time and one static Lua program for an exact preceding-60-second window, and retain fail-closed source, secret, Redis, and deadline boundaries. Fix: added strict local/deployed source contracts, AWS Secrets Manager HMAC and Redis credential snapshots with bounded rotation, atomic 61-slot Lua enforcement and aggregate telemetry, credential-aware Redis/rate-limit caching, and an exact session-only middleware skip that runs after the shared guard while preserving method, auth, identity, and CSRF ordering for other routes.

- `2026-08-21` - `Missing temporary session response diagnostics`: Valid ceiling rejections wrote legacy 429 or 503 responses without recording which bounded decision produced the response. Approach: use the already-attached request logger and retain only the validated response-neutral reason and status fields. Fix: emit `temporary_session_ceiling_response` before either response write, exclude request source data, and cover the structured payload plus log-before-write ordering for both response paths.
- `2026-08-21` - `Temporary session route-safety and review corrections`: The session route's outer cache wrapper hid its approved rate-limit entrypoint from the route-safety check, fail-closed guard integration defects had no structured diagnostics, and two tests overstated cookie-order and infrastructure-cause coverage. Approach: move the route-specific cache header into an opt-in middleware option, retain the direct approved export, log only fixed identifier-free guard failure reasons, and align tests with middleware-owned ordering plus distinct response-neutral decisions. Fix: added early `cacheControl` support, exported the session route directly through `withRateLimit`, covered all five guard failure categories, removed mock-owned cookie ordering, and collapsed duplicate `internal_failure` route cases.
- `2026-08-20` - `Temporary session ceiling v1 integration boundary`: The completed process-local ceiling was not yet attached to the v1 session route, and the donor middleware seam lacked strict result and writer validation while applying private cache policy globally. Approach: add one opt-in fail-closed boundary after method and operation validation, keep cache ownership outside the composed v1 route, and preserve existing Redis and legacy response behavior. Fix: validated guard decisions and callback configuration, closed thrown, rejected, malformed, missing-writer, failed-writer, and partial-response paths, attached the synchronous `v1` ceiling evaluator plus bounded legacy response writer, and added real-route and middleware regression coverage.
- `2026-08-20` - `Temporary session ceiling construction latch logging`: The bounded factory-construction reason was cleared when the request logger was unavailable or threw, preventing a later usable logger from emitting the latch diagnostic. Approach: match rejection sampling's success-aware retention pattern without changing fail-closed enforcement. Fix: clear the construction reason only when `emitBoundedLog` returns true and cover unavailable, throwing, and later usable logger attempts.
- `2026-08-20` - `Temporary session ceiling callback documentation`: The new parameterized dependency-validation regression callback lacked the repository-standard function comment. Approach: document the existing test contract without altering its fixtures or assertions. Fix: added concise parameter documentation plus the crypto non-invocation and bounded-logger expectations above the test block.
- `2026-08-20` - `Temporary session ceiling dependency classification`: Non-callable factory dependencies were mislabeled as HMAC key-generation failures because validation shared the key-generation catch path. Approach: validate every callable seam before invoking the random-byte provider and select diagnostics only from a fixed allowlist. Fix: added a distinct dependency-validation category, limited the HMAC-key category to thrown or malformed key generation, and covered invalid clock, random-byte generator, and HMAC factory cases while proving crypto initialization never begins.
- `2026-08-20` - `Temporary session ceiling scan and construction diagnostics`: The once-per-second cleanup path fully validated every live ring, and factory-time failures entered the unhealthy state without preserving a reason for the first latch event. Approach: keep cleanup limited to bounded expiry metadata while retaining full validation for the addressed request, and carry only a bounded, non-sensitive construction diagnostic through the first unhealthy evaluation. Fix: removed semantic ring validation from the cleanup scan, preserved shape checks and expired-entry deletion, retained per-request validation, and logged a fixed initialization-failure category once before clearing it; focused regressions cover both boundaries and prove injected error text is not logged.
- `2026-08-20` - `Temporary session ceiling review findings`: Cleanup fully scanned stale rings before expiry classification, health latching had no distinct transition signal or snapshot state, failed logger attempts consumed rejection sampling, and factory guards plus the 10,000-source loop lacked efficient direct coverage. Approach: preserve fail-closed enforcement and bounded identifier-free state while tightening only the cleanup, telemetry, and test boundaries. Fix: added lightweight entry metadata validation before expiry, retained full validation for live entries and validate-before-delete cleanup, emitted one health-latch event, exposed boolean health state, retried rejection samples after logger failure, covered invalid factory options, and aggregated indexed load failures after the loop.
- `2026-08-20` - `Temporary session ceiling donor safety defects`: The prior donor used a 60-slot boundary that could reuse a live bucket, retained raw source addresses as state keys, used wall-clock decisions, and could partially delete entries before detecting later corruption. Approach: rebuild only the isolated primitive on current `staging` with conservative inclusive-window arithmetic, transient canonicalization, process-local HMAC identity, monotonic time, and validate-then-delete cleanup. Fix: added a synchronous 400-request ceiling with 61 physical slots, strict raw-header source proof, digest-only bounded state, persistent fail-closed health, count-only telemetry/snapshots, and focused unit/load regressions; the load suite also caught and corrected validation that initially mistook expired-but-unreused physical slots for live corruption.
- `2026-08-17` - `GATE-0 historical status ambiguity`: The contract review presented the evidence-capture branch, base, and draft state under a generic status heading, allowing readers to mistake them for current PR metadata. Approach: preserve the historical values while labeling their provenance and separating them from live promotion state. Fix: renamed the block, added an explicit non-current-status notice, and capture-labeled the review date, branch, base, scope, merge state, and gate state.
- `2026-08-17` - `GATE-0 hosted Auth provenance misclassification`: The contract review grouped hosted Auth server `v2.195.0` with installed-source records even though it came from the deployed capture. Approach: label each version by its frozen evidence source without changing the existing sign-out or chunker observations. Fix: marked the hosted server as deployed-capture evidence and kept `@supabase/supabase-js` `2.90.1` explicitly attributed to both installed-source and deployed-capture provenance.
- `2026-08-17` - `GATE-0 evidence remained provisional after successful capture`: The committed fixtures still held an empty deployed allowlist and nullable cookie cap after the approved hosted run succeeded. Approach: retain only sanitizer-approved provenance and exact operation/class/code/status tuples, with an explicit fail-closed partition for unsupported candidates. Fix: froze the three observed tuples, set the application cookie ceiling to six installed-SSR chunks, recorded the successful run and dependency versions, updated fixture-integrity coverage without retaining raw Auth logs or identifiers, and documented the exact-tuple test's hosted-evidence dependency and fail-closed assertion.
- `2026-08-16` - `GATE-0 missing-user and sanitization assertions`: The secret-key preflight accepted either the expected Auth error code or a 404, allowing an unrelated 404 to pass, while error serialization assertions could not prove that non-enumerable messages and causes excluded provider details. Approach: require the exact missing-user tuple and assert the public error surface directly. Fix: changed the acceptance condition to require `user_not_found` with status 404, covered both one-sided mismatch cases, and pinned every affected `Gate0StageError` to its fixed message, expected stage, and undefined cause.
- `2026-08-16` - `GATE-0 hosted credential failure ambiguity`: Hosted capture used legacy anon/service-role credential names and collapsed all non-cleanup provider failures into one generic terminal message, so a failed run could not identify which safe boundary rejected the deployed new-key configuration. Approach: validate only dedicated publishable/secret keys, prove each credential surface read-only before mutations, and expose only allowlisted stage identifiers. Fix: added management-config, publishable-settings, and guaranteed-missing-user admin probes; reused them in capture; wrapped setup, session-envelope, and scenario failures in finite safe stages; retained distinct cleanup priority and generic unknown-error handling; suppressed dependency output; and added focused regression coverage.
- `2026-08-16` - `GATE-0 main-promotion review findings`: Candidate chunk counts could drift between the evidence guard and contract fixture, optional hosted-version request or JSON failures aborted capture, and CLI diagnostic routing trusted constructor names instead of shared error identities. Approach: centralize measured expectations, treat unavailable version metadata as nullable evidence, and preserve audited diagnostics through exported error classes. Fix: exported and reused both variant chunk counts, derived the candidate maximum, caught health transport and parse failures, switched the CLI to `instanceof` checks with identity-correct isolated tests, documented the built-in-only pre-install refusal proof, and normalized adjacent list formatting.
- `2026-08-16` - `GATE-0 suppression assertion ordering`: The dependency-sink no-call check ran only after console restoration instead of immediately after raw provider sentinels were emitted. Approach: keep the test structure and restoration coverage unchanged while placing the assertion at the behavior boundary it verifies. Fix: moved the existing no-call assertion directly after `withSuppressedDependencyConsole()` returns.
- `2026-08-16` - `GATE-0 evidence runner review gaps`: Dependency output could escape through secondary console methods, cleanup and terminal failures lacked distinct safe handling, private SSR layout loading preceded its version gate, classification rebuilt serializer-backed fixtures, trusted environment strings remained unnormalized, ES256 hosted sessions failed an RS256-only envelope check, and cleanup edge cases lacked direct coverage. Approach: verify each reported path against the current branch and target's public signing-key metadata, then keep changes at the existing output, validation, serializer, classifier, and cleanup-test boundaries. Fix: suppressed and restored all eight console output methods, added fixed cleanup and terminal diagnostics, checked SSR versions before private imports, reused frozen user fields, returned canonical trimmed configuration, allowlisted only ES256 and RS256 token algorithms, and covered cleanup failure plus `markDeleted()` behavior.
- `2026-08-13` - `GATE-0 evidence lacked a safe deployed capture path`: The restored pre-production project still had no auditable way to reproduce the seven provider candidates or bound the largest supported Google session without risking secret-bearing logs, wrong-project mutations, or synthetic-user residue. Approach: make the target, dependency, fixture, credential-shape, output-schema, and cleanup boundaries fail closed before any evidence can be retained. Fix: added an exact-ref manual workflow, installed-SSR six-chunk fixture reproduction, strict oversized-to-unavailable classifier, names-only preflight, read-only Auth policy inspection, exact SDK-surface scenarios, console suppression, sanitized tuple validation, and isolated `try/finally` deletion for every disposable user while leaving the formal Gate-0 values unresolved.
- `2026-08-13` - `GATE-0 sign-out status and chunk-size duplication`: The contract review listed sign-out HTTP pairs absent from the frozen fixtures/schema, and installed chunker evidence repeated the encoded character limit. Approach: align the review with the approved sign-out contract and keep one exported source for the installed chunk size. Fix: limited the review to `400`, `403`, and `405` response pairs and referenced `SUPABASE_ENCODED_CHUNK_SIZE` from the evidence object while preserving its assertions.
- `2026-08-13` - `GATE-0 cookie-cap and protected-page fixture gaps`: The unresolved cookie-cap record occupied the name reserved for CHUNK-2's deployed integer, its integer assertion inspected the record instead of its value, and the consumer matrix could omit a protected page without failing. Approach: separate unresolved evidence naming from the future numeric contract and derive consumer coverage from the protected routable-page policy. Fix: renamed the record to `MAX_AUTH_COOKIE_CHUNKS_EVIDENCE`, asserted its unresolved status and noninteger value directly, added the billing-cancel consumer row, and required every protected routable page to resolve to a matrix source.
- `2026-08-12` - `Ambiguous endpoint-isolation closure ownership`: The delivery manifest split one unresolved review thread's closure ownership between CHUNK-2 and CHUNK-4, leaving no single accountable closer or explicit closure-evidence field. Approach: normalize the thread ledger without changing the other four recorded ownership dispositions. Fix: assigned CHUNK-2 as the sole primary closer, recorded CHUNK-4 sign-out isolation as a dependency, named one evidence location per thread, and added a documentation check requiring one primary closer and one evidence reference for every row.
- `2026-08-12` - `GATE-0 contract fixtures were not independently reviewable`: The quarantined CHUNK-0 branch mixed useful contract material with intentional failures and out-of-scope production changes, while its cookie key depended on an import-time environment value and its unresolved cookie cap was a coercible `null`. Approach: rebuild only strict schemas, sanitized evidence, and integrity tests from current staging; freeze the owner-approved terminal-account policy separately from deployed activation; and make unresolved evidence impossible to mistake for a numeric cap. Fix: added the green standalone fixture suite, a literal sanitized cookie namespace, a nonnumeric CHUNK-2-owned cookie-cap record, an empty deployed tuple allowlist, public-route divergence coverage, contract-review evidence, and the application-ceiling topology caveats without changing production behavior.
- `2026-08-12` - `Error/auth recovery delivery ambiguity`: The numbered chunks were being treated as one-to-one mergeable PRs, leaving an intentionally red CHUNK-0 head and its stacked CHUNK-1 donor unsafe to route into staging. Approach: preserve both PRs as evidence, rebuild every successor from current staging, separate workstream gates from independently green delivery layers, and keep the detailed governing source outside Git. Fix: published only the compact delivery manifest with complete commit/test extraction ownership, five unresolved-thread owners, an open gate ledger, ordered successor/closure rules, the conservative unapproved `user_banned` discrepancy, restricted native-stack policy, and the 400 application versus 1,000/500 WAF ceiling distinction.
- `2026-08-05` - `OAuth failure retry latch coverage`: Provider-failure tests confirmed the action re-enabled but did not prove clearing the pending latch allowed another OAuth handoff. Approach: retry the same restored button after each failure form and assert the provider call contract. Fix: added second-click call-count and second-call `'google'` assertions for returned and rejected provider failures.
- `2026-08-05` - `Login OAuth duplicate initiation race`: Rapid sign-in clicks could enter the OAuth handler twice before the loading state disabled the control. Approach: guard the handler synchronously while preserving retry behavior only for local initiation failures. Fix: added a ref-backed pending latch that remains set after successful handoff, clears after returned or thrown provider failures, and is covered by a same-batch double-click regression.
- `2026-08-05` - `Custom error pages diverged from the public login`: Dedicated status routes and the framework fallback still rendered an isolated light card without the approved particle-wave atmosphere or top-left TrackTheApp brand. Approach: promote the settled login presentation into one shared public shell while leaving safe status copy, response codes, logging, Back, and conditional Retry behavior unchanged. Fix: moved the wave and branded frame into shared components, centered status-specific error content, promoted the dashboard redirect to the login-style primary action, generalized responsive and reduced-motion selectors, and added focused shell, login, error, and stylesheet coverage.
- `2026-08-05` - `Portal motion and rejected OAuth initiation`: Portal-rendered dashboard controls missed the standard motion tokens, and a rejected OAuth initiation left the login action permanently busy without feedback. Approach: extend the existing scoped selector and recover only failed OAuth attempts so successful redirects stay latched. Fix: added the portal motion selector, caught rejected provider promises with the generic alert, restored loading after both failure forms, and added focused stylesheet and page regressions.
- `2026-08-05` - `Public login wave crop and mobile overflow`: The reference wave collapsed into broad dotted fills on wide screens, the centered max-width shell kept branding away from the viewport edge, and the negative decorative offset forced a narrow-screen scrollbar that could wrap the Google label. Approach: preserve the existing themed OAuth and authentication state handling while separating viewport and form positioning and making the particle geometry scale in screen space. Fix: added a responsive 22-strand dotted mesh, viewport-aligned branding, a 512px desktop form, short-landscape sizing, and clipped bottom-edge artwork with focused contract assertions and six-viewport rendered QA.
- `2026-08-04` - `Public login design mismatch`: The Google sign-in route still used an isolated gray canvas, white card, and blue action that did not match the authenticated application's emerald-noir visual system or the approved login reference. Approach: reuse the shared dashboard tokens in a dedicated public theme root while keeping OAuth, callback errors, authenticated redirects, and loading behavior unchanged. Fix: added the framed gradient canvas, responsive brand and OAuth presentation, a static decorative SVG wave, reduced-motion and focus styling, and six page-level regressions covering accessibility and every authentication state.
- `2026-08-04` - `Actions alignment class coverage gaps`: The live Actions heading and loading placeholders relied on layout-critical display and alignment classes that existing tests did not protect. Approach: verify the current markup and extend only the existing alignment tests. Fix: asserted `inline-block` on the heading label and `ml-auto` on the header and cell skeleton placeholders while preserving the translation and alignment checks; 16 focused tests and diff checks pass.
- `2026-08-04` - `Dashboard account and Actions placement mismatch`: The account menu remained inside the Applications panel and the right-aligned Actions content sat too close to the table edge. Approach: separate page-level account controls from application-specific toolbar controls and move only the Actions content without changing column geometry. Fix: rendered ProfileDropdown in its own top-right row, translated the desktop Actions label and menu trigger 20px left, mirrored both changes in DashboardSkeleton, and added focused placement regressions.
- `2026-08-03` - `Dashboard skeleton and responsive accessibility mismatch`: Initial auth loading still rendered the superseded light header/table, the company-search clear target was 28px, the desktop Actions track could clip its 36px trigger, and reduced-motion overrides covered only explicitly marked elements. Approach: reuse the settled dashboard shell and locked breakpoints, keep the visual scaffold decorative, and make only evidence-backed target, density, and motion adjustments. Fix: rebuilt DashboardSkeleton with rail, wide Filters, toolbar, responsive table/cards, pagination, footer, and fixed billing geometry; switched the primitive to emerald tokens; widened Actions from 6% to 8%; enlarged search clear to 36px; scoped reduced motion across dashboard and portal descendants; and added focused regression coverage.
- `2026-08-03` - `Dashboard Add focus ownership race`: A deferred Add Application focus request could survive while Edit or another focus-owning Dashboard overlay opened, then steal focus when saving settled. Approach: cancel deferred Add focus restoration as soon as a competing page-owned overlay becomes active. Fix: included Edit, Delete, Upgrade, Activity, and compact Filters ownership in the restoration effect and added a cancel-to-Edit regression proving focus remains in the active dialog.
- `2026-08-03` - `Dashboard Add focus restoration while saving`: Closing the inline Add form during an active save consumed its focus-restoration request while the toolbar trigger was disabled. Approach: retain the request until both the form is closed and saving has settled. Fix: gated focus restoration on `!saving`, reran the effect when saving changes, and added page-level regression coverage for the disabled-to-enabled transition.
- `2026-08-03` - `Modal dismissal guard follow-ups`: The Edit Cancel button bypassed the active-save dismissal guard, requestClose connections were under-documented, and the fixes guide omitted required heading/list spacing. Approach: keep behavior changes at the shared close callback boundary and document only verified consumers. Fix: routed Cancel through requestClose, disabled it while saving, added regression coverage, expanded Edit/Delete callback documentation, and inserted the missing Markdown blank line.
- `2026-08-03` - `Staging CodeRabbit follow-ups`: Review found undocumented filter-state intent, an interactive closed Activity drawer, mutation-time modal dismissal paths, missing Edit saving-state coverage, hardcoded validation limits, and overly frequent change-log requirements. Approach: verify each current execution path and keep fixes at the affected comment, accessibility boundary, close callback, and assertion. Fix: documented the filter predicates, hid/inerted the closed drawer, guarded Edit/Delete close buttons, backdrops, and Escape during mutations, added focused regressions, interpolated shared validation constants, and aligned `AGENTS.md`, `CLAUDE.md`, and both logs on PR-push-only summaries.
- `2026-08-02` - `Overlay callback effect rationale`: The committed callback synchronization was correct, but its concurrent-render safety constraint was undocumented and could regress to render-phase mutation. Approach: keep behavior unchanged and document why synchronization must remain post-commit. Fix: added a concise rationale above the `onCloseRef` effect so abandoned concurrent renders cannot leak callbacks through a future render-phase mutation.
- `2026-08-02` - `Dashboard review findings`: PR #121 retained an Add-to-Edit focus race, render-time overlay callback mutation, unannounced field errors, duplicate Add action copy, singular archive grammar, declaration spacing drift, and timezone-sensitive date fixtures. Approach: verify every report on the current branch and patch only the affected state boundary, semantics, copy, spacing, and fixtures without changing payloads or rendering behavior. Fix: gated Add focus restoration on live form state, synchronized overlay callbacks after commit, added field-error alerts and distinct Save copy, corrected archive grammar and CSS spacing, stabilized local-date fixtures, and added regression coverage; 64 focused tests, changed-file ESLint, UTC+14/UTC-10 fixture runs, and diff checks pass.
- `2026-08-02` - `Dashboard focused regression gaps`: Chunk 4 presentation lacked promotion-level interaction coverage, DeleteModal forwarded React's click event to its documented no-argument confirm callback, and opening Edit/Delete directly from the Radix row menu let delayed menu cleanup steal focus from the dialog. Approach: add focused real-component coverage across forms, overlays, menus, conditional states, storage, billing, help copy, and shared overlay stacking, then keep production repairs at the two confirmed contract boundaries. Fix: DeleteModal now invokes confirmation with no arguments, JobActionsMenu waits for Radix trigger-focus restoration before opening the selected dialog, and the integration tests prove initial focus, trapping, scroll locking, dismissal, and return focus; 157 focused tests, all 1,305 non-integration unit tests, repository-wide lint, the placeholder-only production build, and diff checks pass.
- `2026-08-02` - `Dashboard implementation plan PR cleanup`: PR #121 unintentionally included the dashboard reference-redesign implementation plan even though the plan was not part of the requested review scope. Approach: remove only the branch-added plan while preserving the dashboard implementation and its documentation. Fix: deleted `docs/plans/dashboard-reference-redesign-implementation-plan.md` from the feature branch so it no longer appears in the PR against `staging`.
- `2026-08-02` - `Dashboard activity and conditional presentation`: Activity, help, storage/archive, Upgrade, and page-local conditional states still used the superseded light or blue dashboard styling, and archive deletion lacked the shared overlay focus stack. Approach: apply dashboard-scoped emerald presentation while preserving date aggregation, archive privacy, billing eligibility/cooldowns, filtering, pagination, and every mutation latch. Fix: added the Activity and conditional-state styling, corrected help copy, introduced a dashboard-only plan-card appearance, connected archive deletion to the shared focus/scroll owner, used canonical filtered-status copy, and validated the patch with changed-file ESLint, 76 focused tests, and diff checks.
- `2026-08-02` - `Dashboard forms and primary overlay infrastructure`: Dashboard forms and primary dialogs still used the superseded light presentation, portal menus could not inherit dashboard tokens, and independently managed overlays could compete for Escape, focus return, and body-scroll ownership. Approach: keep validation, payloads, callbacks, dismissal behavior, and mutation latches unchanged while centralizing only overlay accessibility state and applying dashboard-scoped presentation. Fix: added opt-in portal tokens, a Strict-Mode-safe top-overlay stack with counted scroll locking, Add Application focus return, accessible error associations, and emerald-dark Add/Edit/Delete surfaces; `git diff --check` passes, while focused interaction tests remain intentionally assigned to Chunk 4 Phase 3.
- `2026-08-01` - `Admin users authorization branch coverage`: The Admin users server-render authorization suite covered only authenticated non-admin denial, leaving missing-user redirects and successful admin rendering unverified. Approach: exercise both uncovered `getServerSideProps` branches without changing the existing denial test or production behavior. Fix: added a missing-user `/login` redirect test and an authenticated-admin success test that requires empty props while proving the response remains untouched; all 3 focused tests and diff checks pass.
- `2026-08-01` - `Dashboard presentation and pagination review follow-ups`: Application rows and cards duplicated notes/status derivation, filtered result changes could leave useJobs on an empty out-of-range page, and the pagination controls wrapper carried an ineffective accessible label. Approach: centralize only the shared presentation values, synchronize the source page state with filtered counts, and retain the existing labeled navigation landmark while removing the redundant wrapper label. Fix: both application views now consume one tested 90-character notes/status/date helper, useJobs clamps currentPage after deletion or filter-changing updates, pagination retains its layout with meaningful accessibility coverage, and 39 focused tests, changed-file lint, and diff checks validate the changes.
- `2026-08-01` - `Job action focus-restoration test race`: The row-menu Escape test asserted focus immediately after the menu unmounted even though Radix restores trigger focus in a zero-delay timer, producing a timing-dependent CI failure with focus temporarily on the document body. Approach: preserve the production menu and exact focus assertion while synchronizing the test with Radix's documented macrotask boundary. Fix: the test now awaits one zero-delay timer inside React `act()` before requiring the trigger to own focus; repeated focused runs, combined dashboard coverage, targeted lint, and diff checks validate the fix.
- `2026-08-01` - `Dashboard review follow-up fixes`: Desktop rows hid a valid zero-epoch status date, the account trigger's missing-email fallback lacked direct coverage, and applied search updates could reset a newer toolbar draft and cancel its debounce. Approach: route status dates through the existing safe formatter, cover the existing account fallback without changing menu behavior, and reserve draft invalidation for the page-owned reset key. Fix: zero-valued Added and status dates both render January 1, 1970, the Account fallback and accessible name are pinned by a focused test, and sanitized applied searches no longer overwrite draft text while Clear All still cancels pending work; 54 focused tests, targeted lint, and diff checks pass.
- `2026-08-01` - `Dashboard filter, deletion, and Admin page guards`: Salary-only filters showed the generic no-applications state, rapid delete confirmations could dispatch twice before hook state re-rendered, and direct Admin users page requests relied on client-side role redirects. Approach: preserve staging's current Dashboard toolbar and existing client/API authorization while adding only the missing filter criteria, a synchronous deletion latch, and cookie-backed server rendering authorization through the shared admin guard. Fix: salary bounds now render the filtered empty message, duplicate deletion dispatches are suppressed with retry-safe cleanup, authenticated non-admin page requests receive the standardized 403 before rendering, and validation passes with 29 focused tests, all 1,257 non-integration tests, repository-wide lint, the production build, and diff checks.
- `2026-08-01` - `Application date zero-epoch handling`: The shared formatter treated numeric epoch zero as missing while JavaScript date coercion could accept unsupported input types. Approach: allow only string and numeric date inputs, reject blank strings before construction, and retain the existing invalid-date fallback. Fix: epoch zero now formats as January 1, 1970 in UTC, unsupported and unavailable values return the em dash, and focused utility/component coverage locks the contract; 25 tests, targeted lint, and diff checks pass.
- `2026-08-01` - `Dashboard application-date and button hardening`: Desktop and mobile application views duplicated identical date formatting, while the dashboard error Dismiss control relied on the default button type. Approach: extract only the repeated formatter without changing its fallback or UTC semantics, and make the Dismiss control explicitly non-submitting while preserving its handler. Fix: added the shared application-date utility and focused UTC/fallback coverage, imported it in both application views, set Dismiss to `type="button"`, and covered its type plus clear-error behavior; 45 focused tests, targeted lint, and diff checks pass.
- `2026-07-30` - `Dashboard workspace toolbar, results, and pagination`: The applications workspace retained the superseded toolbar, split desktop columns and direct row actions, and pagination without accurate result ranges. Approach: preserve Dashboard-owned filtering and mutation handlers while extracting focused emerald-noir presentation components, retaining the approved 300ms sanitized company-search debounce, and using Radix only for compact menus. Fix: added the Applications toolbar, email-only account control, responsive six-column table and direct-action cards, canonical status treatments, safe dates and expandable notes, guarded Edit/Delete menus, and defensive fixed-size pagination; 59 focused tests, all 1,242 non-integration tests, full lint, the production build with documented CI placeholders, and diff checks pass.
- `2026-07-30` - `Maximum salary debounce reset coverage`: The Clear All Filters regression test proved cancellation only for the minimum-salary timer even though maximum salary uses a separate timer and callback. Approach: queue pending minimum and maximum edits in the same test and assert both callbacks before and after the debounce boundary. Fix: verified both inputs clear immediately, both callbacks emit `null` exactly once, and neither stale salary value is submitted after 300ms; the focused suite, changed-test lint, and diff checks pass.
- `2026-07-30` - `Pending salary filter clear race`: A newly entered salary was held only in local state until its debounce expired, so Clear All Filters stayed hidden and could not cancel the queued update. Approach: include the immediate local salary values in the existing filter-presence calculation and exercise the pre-debounce reset path. Fix: show Clear All Filters for pending minimum or maximum salary edits, clear the salary timer through the existing reset handler, and verify no stale salary update fires after 300ms.
- `2026-07-30` - `Filters promotion review findings`: Latest Status Change formatting depended on the runtime timezone, and the salary-clear and unmount-cancel debounce branches lacked direct coverage. Approach: preserve the existing locale and debounce implementation while pinning one shared calendar and exercising the uncovered timer boundaries. Fix: set the rendered date to UTC, asserted the exact locale options, verified an emptied minimum salary emits `null` only after 300ms, and proved unmount cancels a pending search callback.
- `2026-07-30` - `Dashboard responsive review coverage`: Responsive Filters and Activity behavior had low-risk gaps where mocked or self-fulfilling tests could miss breakpoint drift, drawer cleanup, boundary handling, hidden-container semantics, and explicit focus restoration. Approach: preserve production behavior and strengthen only the smallest real-component and page-level contracts. Fix: covered live drawer-to-docked transitions with pending inputs, tied the React media query to Tailwind's `wide` screen, asserted salary clamping and HTML search sanitization, verified the closed Activity wrapper remains hidden and inert, and moved focus away before proving the docked Filters close handler restores it; the focused suites, changed-test lint, and diff checks pass.
- `2026-07-30` - `Dashboard navigation and responsive filter ownership`: Navigation, billing entry presentation, and responsive Filters controls were split across toolbar and viewport-specific UI paths, leaving no collapsible wide rail and risking duplicate filter state. Approach: retain one page-owned state model and one mounted Filters component while separating wide disclosure state from the compact drawer and preserving canonical billing decisions. Fix: added the responsive TrackTheApp navigation, animated zero-width filter track, docked/drawer accessibility semantics, focus and scroll handling, state-preservation coverage, and a compact-safe media-query lifecycle; 58 focused tests, changed-file lint, Tailwind compilation, and diff checks pass.
- `2026-07-30` - `Dashboard foundation review follow-ups`: The auth-loading page test did not prove DashboardSkeleton rendered inside DashboardShell, and the reduced-motion override retained the skeleton's 150ms animation delay. Approach: expose a distinctive test-only marker and extend the existing PostCSS contract without changing application flow. Fix: asserted the skeleton marker under `.dashboard-root`, set `animation-delay` to important zero in the shared reduced-motion rule, and verified both its value and importance.
- `2026-07-29` - `Dashboard redesign foundation`: The authenticated dashboard lacked the approved emerald-noir design tokens, scoped typography, atmosphere, and responsive shell needed by later redesign chunks. Approach: introduce an additive dashboard-only foundation while preserving the existing auth, jobs, filters, billing, modal, drawer, and tooltip ownership. Fix: added the scoped Inter shell, semantic Tailwind and CSS tokens, static light-field and vignette treatment, responsive minmax geometry, reduced-motion primitives, approved Lucide and Radix dependency records, and focused font-loader coverage; 18 focused page tests, the Tailwind compile, lockfile dry-run, production audit, contrast checks, and `git diff --check` pass, while the production build remains blocked locally by the four required build environment names.
- `2026-07-28` - `Sharp smoke PNG signature validation`: The image optimizer smoke accepted any non-empty body labeled `image/png`, so a mislabeled error payload could satisfy its response checks. Approach: keep the existing status and cache contract while sharing the body checks across both live responses and a negative fixture. Fix: required the exact eight-byte PNG signature after the non-empty and Content-Type checks, applied the assertion to both responses, and added a non-empty spoofed `image/png` fixture that must be rejected.
- `2026-07-27` - `Ubuntu Sharp smoke CRLF header matching`: The first PR run returned HTTP 200 with a non-empty optimizer body but rejected its valid response headers because curl writes CRLF lines and the single-quoted GNU grep expressions treated `\r` as a literal `r`, not a carriage return. Approach: preserve the exact PNG and cache assertions while making their optional line-ending match explicit in Bash. Fix: changed all four header expressions to ANSI-C quoting so `\r` becomes an actual optional carriage return; synthetic LF and CRLF headers and the exact clean-cache local smoke now pass, with the Ubuntu rerun providing the final confirmation.
- `2026-07-27` - `Development dependency security follow-up`: Six vulnerable development-only lockfile entries remained after the production dependency remediation. Approach: refresh each transitive package to the newest version allowed by its existing parent range, keep the manifest and supported dependency contracts unchanged, and add an Ubuntu runtime smoke that exercises Next image optimization through Sharp without external requests. Fix: updated `form-data` to `4.0.6`, `js-yaml` to `3.15.0`, `tmp` to `0.2.7`, `@babel/core` and its coupled Babel family to `7.29.7`, and `@tootallnate/once` to `2.0.1`, fully patching those five package lines; updated `brace-expansion` from `1.1.12` to `1.1.16`, fixing its two earlier advisories while retaining one residual advisory.
  - Audit result and reason for 27 entries: on `2026-07-27`, the explicit public-registry production audit reports 0 findings and the complete-tree audit reports 27 high-severity package entries. The complete audit contains exactly one advisory object: `GHSA-mh99-v99m-4gvg` on development-only `brace-expansion@1.1.16`. npm expands that one leaf advisory into 26 meta-vulnerability records through `minimatch`, ESLint packages and plugins, glob, Jest packages, Babel/Jest integration packages, and their dependent development tools. The displayed 27 therefore represent one root advisory plus its propagated dependency paths, not 27 independent vulnerabilities and not a production dependency path.
  - Residual decision: ESLint `9.39.5` reaches the residual through `minimatch@3.1.5`, whose `brace-expansion` range is `^1.1.7`; patched `brace-expansion@5.0.8` does not satisfy that range. A range-breaking override would create an unsupported dependency combination, while reverting to `1.1.12` would restore two additional known advisories. ESLint 10 is a separate semver-major compatibility project, so the supported in-range `1.1.16` resolution and its documented development-only residual were retained without `npm audit fix`, `--force`, a new override, or advisory suppression.
  - Assurance: CI now starts the built application on Ubuntu, waits on the static favicon, makes two local `/_next/image` requests, and requires non-empty HTTP 200 `image/png` responses with cache `MISS` followed by `HIT`, while always cleaning up the server. A clean install, dependency-tree check, production and complete audits, zero-warning lint, all 1,203 CI unit tests, the production build, workflow YAML and Bash syntax checks, and the equivalent clean-cache local Sharp runtime smoke pass; the Ubuntu execution remains the required post-push confirmation.
- `2026-07-27` - `Production dependency vulnerability remediation`: The production lockfile contained five findings (four high and one moderate), including unsupported Next 14 and actively used sanitization/build dependencies. Approach: upgrade to supported Next `16.2.12`, migrate the lint toolchain to ESLint `9.39.5` plus `eslint-config-next` `16.2.12`, preserve React/React DOM `18.3.1`, update safe transitive packages, and use the explicitly approved Next-scoped PostCSS `8.5.18` and Sharp `0.35.3` overrides because the latest stable Next still publishes vulnerable native ranges.
  - Fix: added direct `@eslint/js` `9.39.5` and `globals` `16.5.0`, migrated the legacy ESLint config to flat config, retained the established Rules of Hooks and exhaustive-dependencies contract, restored ESLint 8 catch-parameter handling, and disabled the 14 compiler-era React rules rather than changing application behavior across 18 files. Important transitive resolutions are DOMPurify `3.4.12`, Undici `7.29.0`, and WS `8.21.1`.
  - Security result: the live public-registry production audit improved from 5 findings to 0; the complete-tree audit improved from 41 findings to 6. The residual development-only findings are high-severity `brace-expansion`, `form-data`, `js-yaml`, and `tmp`, plus low-severity `@babel/core` and `@tootallnate/once`. Each affected lockfile entry is marked `dev: true`, has no production audit path, and reports a fix as available; further development-tree upgrades remain outside the approved zero-production-finding scope, with no advisory suppression or forced install used.
    - `brace-expansion@1.1.12` - high; GHSA-f886-m6hf-6m8v, GHSA-3jxr-9vmj-r5cp, and GHSA-mh99-v99m-4gvg through `eslint -> minimatch -> brace-expansion`; development-only; fix available.
    - `form-data@4.0.5` - high; GHSA-hmw2-7cc7-3qxx through `jest-environment-jsdom -> jsdom -> form-data`; development-only; fix available.
    - `js-yaml@3.14.2` - high; GHSA-h67p-54hq-rp68 and GHSA-52cp-r559-cp3m through `jest -> @jest/core -> @jest/transform -> babel-plugin-istanbul -> @istanbuljs/load-nyc-config -> js-yaml`; development-only; fix available.
    - `tmp@0.2.5` - high; GHSA-ph9p-34f9-6g65 through `patch-package -> tmp`; development-only; fix available.
    - `@babel/core@7.28.6` - low; GHSA-4x5r-pxfx-6jf8 through direct development Babel plugins, `eslint-config-next -> eslint-plugin-react-hooks`, and Jest transform/configuration paths; development-only; fix available.
    - `@tootallnate/once@2.0.0` - low; GHSA-vpq2-c234-7xj6 through `jest-environment-jsdom -> jsdom -> http-proxy-agent -> @tootallnate/once`; development-only; fix available.
  - Validation: `npm ci` completed with no invalid or unmet peers; effective ESLint configs were verified for middleware, React source, and Jest files; lint passed with zero warnings; all 1,203 CI unit tests and 86 focused middleware/error-page/API/validation tests passed; the Next `16.2.12` Turbopack production build compiled every Pages Router page and API route plus middleware; placeholder-only runtime checks returned the intended custom error statuses, redirected protected navigation to login, kept API failures sanitized and fail-closed, served the favicon, and optimized it through Sharp with cache reuse; `git diff --check` passed.
  - **Future maintainability risk:** moderate unless the disabled rules are revisited before adopting React Compiler or making a major React upgrade.
- `2026-07-26` - `Unenforced protected-branch CI gate`: The stable secret-free CI workflow was green on both branch routes but neither active ruleset required its check, so a failing pull request was not durably gated by repository settings. Approach: derive the exact check identity from successful GitHub Actions check runs, preserve both complete rulesets while adding only that check, and use synchronized temporary never-merge PRs to prove failure and repair behavior. Fix: rulesets `19072143` and `19072163` now require GitHub Actions `unit-lint-build` (integration `15368`), with staging non-strict and main strict; PRs #99 and #100 were blocked by deliberate failures, became clean after repair, and were closed unmerged, with no Trusted Integration, Amplify, deployment, or application behavior changes.
- `2026-07-25` - `Staging-to-main documentation conflicts`: Staging and main independently prepended release-history entries, leaving the promotion PR conflicted in both shared logs. Approach: merge current main into a staging-based resolution branch and reconcile only the two documentation blocks while retaining main's non-conflicting history. Fix: preserved every unique readiness, billing-cleanup, baseline, and CodeRabbit entry once and removed only duplicated shared context; lint, all 1,203 unit tests, and `git diff --check` pass.
- `2026-07-25` - `Missing billing cleanup guard regression coverage`: The both-id baseline cleanup guard had no direct coverage for either partial-id state or the complete-id success path. Approach: extract a dependency-injected test seam inside the billing migration suite so guard behavior runs without integration credentials while the real suite keeps the same cleanup flow. Fix: added independent missing-user-A and missing-user-B assertions proving the runner and billing callbacks stay uncalled, plus a success assertion proving the runner and both callbacks execute; three focused tests and all 1,203 unit tests pass, 45 credential-gated tests skip safely, and `git diff --check` passes.
- `2026-07-25` - `Unset baseline billing cleanup ids`: Partial baseline auth setup could leave one user id unset while `clearBaselineRows` still registered both billing cleanup callbacks. Approach: require both recovered baseline ids before invoking the shared all-attempted cleanup runner, so partial setup skips billing-row cleanup entirely. Fix: added the minimal early-return guard and recorded the behavior; all 1,203 unit tests pass, the focused billing integration suite loads and safely skips 45 credential-gated tests, and `git diff --check` passes.
- `2026-07-24` - `Migration readiness diagnostics gaps`: Migration-suite auth cleanup reused one label for two users, the remote-catalog assertion filtered out unexpected migration versions, cleanup-label deduplication lacked regression coverage, the focused callback lacked repository-standard documentation, and the test did not prove duplicate-labeled callbacks still ran independently. Approach: preserve teardown callbacks and the cleanup runner, distinguish users with names-only A/B labels, compare the complete ordered catalog to `CANONICAL_MIGRATIONS`, add one focused duplicate-label test, document its mocks, rationale, and side effects, and assert each registered mock runs once. Fix: updated the migration suite, cleanup-runner coverage, test callback documentation, and independent callback-execution assertions; seven focused cleanup tests pass, the credential-gated migration suite loads and skips safely without infrastructure, and `git diff --check` passes.
- `2026-07-23` - `Trusted integration readiness gaps`: The staging workflow lacked an explicit offline wrong-target proof, approved destructive suites could hide rejected or returned-error teardown work, schema tests referenced historical columns, Redis evidence keys lacked workflow correlation, and no restricted test-only SQL helper was versioned outside migrations. Approach: reuse the real target validator, centralize names-only all-attempted cleanup, align tests to the canonical migration/catalog contract, and isolate arbitrary SQL behind a service-role-only helper with pgTAP coverage. Fix: added the pre-install refusal gate, cleanup runner and suite wiring, canonical column assertions, run/attempt plus UUID Redis keys, and the standalone helper contract; local migration replay, 72 baseline assertions, 19 helper assertions, lint, 1,202 CI tests, secretless integration discovery, and the production build pass.
- `2026-07-23` - `Broad public-object default privileges`: The authoritative baseline granted all privileges on future public tables, sequences, and functions to `anon` and `authenticated`, while PostgreSQL's built-in function default also left future functions executable through `PUBLIC`. Approach: preserve explicit `postgres` and `service_role` access while making future client access fail closed at the default-ACL boundary. Fix: replaced the client-role grants with explicit revokes, added the required global `PUBLIC` function-execution revoke, and added a disposable-object pgTAP guard; the full migration replay and all 72 database assertions pass.
- `2026-07-23` - `Canonical Supabase baseline artifacts were unpublished`: The reviewed authoritative baseline, reconciliation migration, local configuration, and final-state pgTAP contract existed only in an untracked-files stash, so the empty integration-test project had no version-controlled migration chain to apply. Approach: recover only the reviewed artifacts byte-for-byte from stash commit `3e06368f`, preserve the recorded fingerprints, and keep the test-only `exec_sql` helper out of the deployable migration chain. Fix: added the five canonical Supabase files with matching Git blob identities and SHA-256 fingerprints for the baseline, reconciliation, and pgTAP artifacts; retained the reconciliation artifact's reviewed trailing blank line as the sole approved `git diff --check` exception.
- `2026-07-23` - `CodeRabbit parsed package metadata as JavaScript`: CodeRabbit's managed ESLint invocation included the valid root `package.json`, which inherited the Next.js JavaScript parser and failed with a misleading missing-semicolon error. Approach: exclude only the root package manifest from ESLint while preserving the existing source-file lint command and all dependency declarations. Fix: added the root-scoped `/package.json` pattern to the existing `ignorePatterns`, verified direct package lint exits without a parse failure, and passed project lint plus all 1,189 CI unit tests.
- `2026-07-22` - `Trusted integration token default was implicit`: The workflow had explicit job permissions but no workflow-level default deny, so future jobs could inherit broader token defaults. Approach: add a top-level zero-permission baseline while preserving each existing job override. Fix: added `permissions: {}` beside the workflow trigger and validated the parsed top-level, guard-job, and integration-job permission values.
- `2026-07-22` - `Missing trusted integration workflow gate`: Destructive integration tests had no trusted GitHub Actions path that rejected wrong refs, missing canonical secrets, mismatched Supabase targets, or deployment-credential fallbacks before Jest loaded remote suites. Approach: separate a secret-free exact-staging guard from an Environment-bound serialized job and centralize fail-closed validation in a dependency-free runner. Fix: added the manual SHA-pinned integration workflow, names-only preflight, secret-backed independent project reference, child-only destructive opt-in and run identifier, focused subprocess/workflow coverage, and verification through 1,189 unit tests plus the safe no-secret integration run.
- `2026-07-22` - `CodeRabbit CI and regression hardening`: Superseded CI runs consumed runner time, the storage-downgrade billing destination lacked regression coverage, and control-only plan values produced blank-looking error labels. Approach: verify each finding against the PR branch and make only the workflow, assertion, and sanitizer-order changes needed. Fix: added per-PR/ref CI cancellation, asserted the Review billing anchor targets `/billing`, moved final plan-label trimming after control and whitespace normalization, and covered control-only input with focused tests.
- `2026-07-22` - `Missing secret-free pull-request CI gate`: Pull requests and protected-branch pushes had no stable unit, lint, and build check that could run without trusted infrastructure credentials. Approach: keep untrusted CI separate from later integration and deployment workflows, pin the Node and Action toolchain, and establish a zero-warning lint baseline with behavior-preserving cleanup. Fix: added the SHA-pinned `CI / unit-lint-build` workflow for `staging` and `main`, exact lint dependencies, deterministic unit execution, build-step-only non-secret placeholders, workflow safety checks, and local evidence from 1,172 passing tests plus a successful production build.
- `2026-07-21` - `Unresolved billing status required a browser reload`: The explicit checkout ERROR state preserved patient payment-status copy but was omitted from the success page manual-refresh condition. Approach: reuse the existing guarded refresh action without changing the unresolved-state copy or status interpretation. Fix: included ERROR in the refresh-button condition and added page-level coverage proving a refresh issues another status request and can transition to active.
- `2026-07-20` - `Billing success terminal outcome separation`: Missing session ids, rejected status requests, and explicit unresolved status shared one error outcome and customer message. Approach: keep customer-facing details vague while separating terminal failures from the unresolved payment state. Fix: added a distinct terminal-error outcome for hard failures and invalid responses, reserved the payment-status wait copy for explicit error status, and covered missing ids, rejected requests, terminal copy, and polling shutdown.
- `2026-07-20` - `Billing success polling details exposed`: Customers waiting for payment status resolution could see internal polling timings and a confirmation-failure message. Approach: preserve the bounded status-check flow while limiting the UI to patient customer-facing copy. Fix: removed the polling schedule card, changed the unresolved error outcome to "Please wait for payment status to update", and added page-level regression coverage for both the copy and hidden timings.
- `2026-07-20` - `Dashboard Billing label flash on initial load`: Authenticated Dashboard rendering began before the storage summary resolved, so the missing status used the fail-closed Billing fallback and then changed to Upgrade for confirmed Free users. Approach: distinguish the initial unresolved request from settled unavailable states without weakening canonical billing-state handling. Fix: render a fixed-size, non-interactive billing-entry skeleton only while loading with no summary, preserve resolved labels during refetches, retain Billing for settled failure states, and add Free, Premium, and refetch transition coverage; all 1,170 unit tests pass.
- `2026-07-19` - `Premium restore RPC migration drift`: Existing pre-production databases retained migration 019's original three-argument restore function after the repository edited that already-applied migration to use a four-argument price allowlist, causing Premium storage reconciliation to fail with PostgREST `PGRST202`. Approach: add a new idempotent forward migration that installs the reviewed hardened function before removing the obsolete overload, reasserts its service-role-only boundary, verifies the final catalog, and reloads PostgREST metadata. Fix: added migration 028, updated the dependent restore/bulk-delete/final-state suites, added a destructive legacy-upgrade integration suite plus an always-on SQL contract test, and completed static diff, syntax, whitespace, and function-body parity checks; Jest and database execution remain intentionally deferred to the dedicated test session.
- `2026-07-19` - `Billing action auth recovery coverage and documentation`: Checkout auth recovery was covered only when the response included an `UNAUTHORIZED` code, and the page handlers' terse comments omitted their loading, hook, redirect, and auth-recovery behavior. Approach: add one page-level status-only 401 regression case and expand the existing handler JSDoc without changing production behavior. Fix: verified the status-only failure signs out, replaces the route with `/login`, and keeps raw details out of rendered content; documented the Checkout and Portal hook hand-offs and unauthorized recovery path.
- `2026-07-19` - `Billing page shared-action migration`: The Billing page retained its own Checkout nonce, action latch, request, and redirect wrapper after the reusable billing-actions hook was introduced, leaving duplicate client orchestration and no page-level Retry-After presentation. Approach: migrate Checkout and portal to one shared hook instance while preserving canonical status, storage, CTA, and auth behavior. Fix: removed the page-local action implementation and obsolete compatibility adapter, separated status and sanitized action errors, honored shared cooldowns, and added regression coverage for duplicate and competing actions, fresh-nonce retries, authorization recovery, and rate limiting.
- `2026-07-19` - `Stale Dashboard skeleton line references`: Dashboard integration moved the matching header, toolbar, and footer blocks while the skeleton comments still pointed to their old source lines. Approach: verify each referenced block against the current Dashboard before changing documentation. Fix: updated the three line ranges without altering skeleton markup or geometry and reran the focused skeleton suite.
- `2026-07-19` - `Dashboard billing entry and dropdown recovery gap`: The Dashboard Resume control navigated to a nonexistent profile route, while Billing lived globally in ProfileDropdown instead of reflecting the user's current billing state. Approach: consume the frozen Premium catalog and exhaustive storage-status mapper, keep Checkout inside the controlled upgrade modal, and preserve a fail-closed Billing route for ambiguous states. Fix: replaced Resume with mapped Upgrade, Manage plan, or Billing behavior, wired modal close/auth/Billing callbacks with overlay protection, removed the dropdown Billing link without changing Admin or Sign Out, and added focused integration coverage.
- `2026-07-19` - `Billing-status schema boundary test gaps`: The billing-status schema enforced the entitlement allowlist and strict response shape, but its rejection matrix did not lock either boundary against regression. Approach: extend the existing table-driven coverage with otherwise-valid Free snapshots that isolate each schema rule. Fix: added explicit unknown-entitlement and unexpected-field cases and verified both fail parsing.
- `2026-07-19` - `Inconsistent upgrade entitlement snapshots`: The shared billing-status schema accepted responses where `entitled` contradicted whether a canonical entitlement was present, allowing malformed Free-shaped responses to reach checkout eligibility and enable Upgrade. Approach: enforce the entitlement flag/value invariant at the shared response boundary while preserving the existing subscription/status consistency rule. Fix: reported contradictory entitlement data on `entitlement`, covered both permutations in the schema and upgrade-modal suites, and kept malformed responses in the retryable state without an Upgrade action.
- `2026-07-18` - `Malformed upgrade eligibility billing snapshots`: The Dashboard upgrade modal accepted any non-array object from the billing-status response, so empty, incomplete, or incorrectly typed snapshots could reach local eligibility checks and enable Checkout. Approach: validate the full canonical billing-status response contract before evaluating eligibility while retaining the existing checkout rules for valid snapshots. Fix: added a strict shared billing-status schema with canonical subscription-status consistency, routed parse failures to the modal's retryable error state, and covered malformed responses plus valid canceled and incomplete-expired checkout paths.
- `2026-07-18` - `Premium upgrade modal status and lifecycle gap`: The Dashboard upgrade flow lacked a reusable accessible modal and could not safely distinguish a stale Free presentation hint from canonical Checkout eligibility. Approach: separate plan presentation from the modal controller, re-read local billing status per open session, and delegate Checkout to the existing shared action hook. Fix: added the Premium Features card and upgrade modal with request-generation race protection, typed 401 and 409 routing, preserved Retry-After feedback, focus trapping and restoration, busy-dismissal guards, and focused component coverage.
- `2026-07-18` - `Malformed billing response metadata coverage`: Billing redirect mapper tests covered only valid HTTP status and Retry-After metadata, leaving sanitizer boundaries vulnerable to undetected regression. Approach: extend the existing parameterized error-mapping matrix with representative malformed values while retaining known safe error codes and messages. Fix: added negative, fractional, and unsafe-integer cooldown cases plus below- and above-range HTTP statuses, asserting only malformed metadata normalizes to `null`.
- `2026-07-17` - `Stale billing redirect and nonce fallback gaps`: A completed billing request could still navigate after its hook unmounted, and a throwing or malformed `randomUUID()` prevented use of an otherwise available secure random-bytes fallback. Approach: add a lifecycle predicate at the final redirect boundary, honor non-redirected executor outcomes in the hook, and isolate both browser entropy paths while retaining fail-closed behavior. Fix: stale handoffs now return an ignored result without navigation, secure UUID failures fall through to `getRandomValues()`, and focused tests cover unmount, portal-specific failures, and legacy Billing-page error recovery.
- `2026-07-17` - `Shared billing action and retry metadata gap`: Checkout and portal orchestration exposed string-only errors from a Billing-page helper, while the planned upgrade modal needed shared duplicate-action protection and preserved `429` metadata. Approach: centralize secure nonce creation, safe response mapping, exact redirect validation, and cross-action state in one reusable hook while retaining a temporary Billing-page compatibility wrapper for Chunk 5. Fix: added typed sanitized outcomes, synchronous Checkout/portal mutual exclusion, fresh nonce retries, active Retry-After countdown preservation and cleanup, navigation-failure recovery, and focused plus Billing-page regression coverage.
- `2026-07-17` - `Plan catalog invalid-limit coverage`: The catalog guard rejected non-positive and non-safe-integer limits, but its focused validation test covered only missing and zero configuration. Approach: expand the existing table-driven matrix without changing production behavior. Fix: added negative, fractional, NaN, and unsafe-integer cases while retaining the tier-specific failure-message assertion.
- `2026-07-17` - `Dashboard Premium entry contract gap`: The dashboard upgrade work lacked a reusable plan catalog and an exhaustive storage-status mapping, which could let customer copy drift or future integration mishandle ambiguous states. Approach: derive display limits from shared tier configuration, expose only frozen UI metadata, and route every non-terminal-Free or unknown status toward Billing. Fix: added the Premium catalog and pure dashboard entry helper with focused coverage for all canonical statuses, missing configuration, unknown values, and prototype-like inputs.
- `2026-07-16` - `Unprotected production branch workflow`: The repository had no persistent staging branch or active branch rulesets, so ordinary changes and production promotion did not have separate protected paths. Approach: create `staging` from the current remote `main`, apply exact-branch pull-request protections without premature CI requirements, and document the solo-owner promotion and emergency process. Fix: added active `staging-protection` and `main-production-protection` rulesets, retained `main` as the default branch, and added the repository promotion policy and pull request checklist.
- `2026-07-16` - `Supabase mismatch integration suite selection`: The CSRF and rate-limit integration suites still registered their tests when canonical credentials existed but the configured Supabase URL did not match the isolated test project. Approach: keep the existing setup and cleanup guards while deriving suite registration from both infrastructure readiness and the shared project matcher. Fix: selected `describe.skip` for missing infrastructure or a mismatched project and verified both suites skip with mismatched dummy configuration while the shared helper tests pass.
- `2026-07-16` - `Integration environment CodeRabbit hardening`: Destructive integration suites could act without consistently proving the isolated Supabase target, repeated test bootstrap values could drift, and sequential Stripe verification waits could each consume the full configured timeout. Approach: verify every review finding against the current branch, centralize the shared test-environment contract, fail closed before privileged test mutations, and preserve one overall post-Checkout wait budget. Fix: added project-identity guards, shared CSRF/bootstrap constants and suite registration, focused fallback coverage, bounded and throttled Stripe harness requests, applied remaining-time polling through receipt verification, and corrected the feature log heading spacing.
- `2026-07-13` - `Missing Phase 0 migration origins`: Root migrations 001-004 were unavailable, leaving the historical user-profile, tailor-cache, abuse-counter, and daily-spend definitions unreviewable. Approach: reconstruct functional equivalents from the authoritative 2026-07-13 PostgreSQL catalogs, preserve the observed RLS, policy, grant, trigger, index, and cascade behavior, and label the files as reconstructions rather than recovered originals. Fix: added the four provenance-labeled SQL migrations and verified them through an atomic local replay, all 71 authoritative pgTAP assertions, and error-level database lint.
- `2026-07-13` - `Jest ignore-pattern override`: The unit and CI script overrides replaced Jest's configured generated-directory exclusions, while single-backslash dot escapes broadened the intended regexes. Approach: preserve every configured ignore pattern when adding the integration-test exclusion and normalize literal-dot escaping in JavaScript strings. Fix: passed all six patterns through both CLI overrides, corrected the config escapes, confirmed each script resolves six exclusions, and passed all 1,031 unit tests.
- `2026-07-12` - `Deterministic CI and build harness`: Jest scanned repo-local worktrees, infrastructure suites could fail before their skip guards, page tests were compiled as production routes, unit baselines had stale environment/contract expectations, and successful health probes retained losing timeout handles. Approach: isolate discovery to the active source tree, separate secret-free unit/CI commands from visibly guarded integration runs, validate build-required environment names without values, move page tests outside `src/pages`, and keep repairs at the smallest verified test or timer boundary. Fix: added stable test/build scripts and preflight coverage, deferred full-pipeline imports until configured, removed a stale auth-schema suite, aligned the safe session-role contract, relocated all page tests with production imports, cleared health-check timers on every race outcome, and verified 1,031 unit tests plus the production build with zero test routes.
- `2026-07-12` - `Staging CI/CD baseline and clean install`: Release work lacked a clean current-main checkout and consistent Node 22 contract, Amplify still documented Node 18, and the DOMPurify 3.3.1 patch no longer applied to locked 3.3.3. Approach: isolate the work on a current `origin/main` linked worktree, adopt the owner-approved rolling Node 22 policy with visible runtime evidence, keep transient branch/worktree inventory in local-only staging evidence, and verify the sanitizer guard upstream. Fix: added `.nvmrc` plus package engine metadata, made Amplify install/use Node 22 and print versions, recorded the baseline locally without publishing machine history, removed the obsolete patch, and verified a clean `npm ci` with an unchanged lockfile and zero reported vulnerabilities.
- `2026-07-12` - `Complete-list pagination and export test hardening`: A server-capped non-empty page could prematurely complete job-list pagination, and Suite K parsed CSV before proving export success. Approach: require an empty page to confirm list completion, assert the export envelope before CSV parsing, and document terminal-only GitHub pushes with the fixes log updated first. Fix: removed short-page termination while preserving the absolute retained limit, added capped-page regression coverage, reordered the integration assertions, updated agent guidance, passed all 60 focused service tests, safely loaded the destructively gated suite, and passed diff checks.
- `2026-07-11` - `Complete-list pagination follow-up hardening`: Partial job-list pages performed an unnecessary confirmation read, and the new destructive Suite K harness had configuration, teardown, lifecycle-documentation, and CSV identity-coverage gaps. Approach: stop below-limit partial pages while preserving exact-limit overflow confirmation, make destructive setup fail clearly, isolate all fixture cleanup attempts, and verify every exported identity. Fix: updated the list loop and focused service tests, hardened and documented the integration lifecycle, asserted all unique rows through row 1001, and validated safe integration skips plus the 59-test job service suite.
- `2026-07-11` - `Bounded complete job-list reads`: Unpaginated dashboard and CSV export reads needed to return every retained job without coupling query transport size to the Premium storage quota or accepting malformed continuation cursors. Approach: set the global retained/Premium limit to 1,000, page internal reads with a shared deterministic `(created_at, id)` cursor, and fail closed when the retained-row invariant or cursor contract is violated. Fix: added shared cursor validation, bounded dashboard/export loops, derived storage constants, focused unit coverage, and a destructively gated 1,000/1,001-row database integration suite on `agent/job-data-performance-chunk3-1000`.
- `2026-07-10` - `Equal-time Stripe retry monitoring`: Intentional fail-closed cross-subscription ties could repeatedly fail webhook receipts without a dedicated retry signal. Approach: preserve the verified retry contract while making both target changes and repeat deliveries alertable. Fix: added the structured `billing_failed_webhook_receipt_retry` warning, locked the existing target-change warning in tests, and pushed the focused monitoring commit to `agent/chunk2-equal-stripe-events`.
- `2026-07-10` - `Same-second Stripe subscription conflicts`: Distinct Stripe events with the same second-level timestamp could let a delayed nonterminal snapshot overwrite a terminal subscription. Approach: move equal-time ordering into the locked database RPC, fail closed on ambiguous cross-subscription ties, and reconcile same-subscription conflicts through the exact local id/version guard. Fix: added migration `027`, explicit service/dispatcher outcomes, strict retryable receipt handling, central unresolved-conflict protection, and focused plus gated concurrency coverage.
- `2026-07-10` - `Empty authoritative sync purpose`: Empty and space-only purpose strings normalized to SQL NULL and bypassed the allowed-purpose check. Approach: reject NULL before evaluating the purpose allowlist and make the migration detector require the corrected function body. Fix: added the explicit NULL guard plus empty and whitespace-only RPC coverage, then verified the destructive billing migration suite passes.
- `2026-07-10` - `Snapshot-guarded Stripe sync clean PR`: Earlier PR attempts inherited unrelated history and conflicted with current main in repository guidance, feature logs, salary migrations, job-hook tests, and rate limiting. Approach: rebuild from current `origin/main`, apply only the Chunk 1 commit, and resolve the feature log in favor of current-main behavior plus the new security entries. Fix: verified the other conflict-prone files exactly match main, retained current latency behavior, reran 179 focused tests, and prepared a ready-for-review replacement branch.
- `2026-07-09` - `Custom error page hardening`: Follow-up review findings covered drift-prone error status allowlists, dropped framework error observability, and repeated status-page boilerplate. Approach: centralize shared status/page helpers and keep rendering props client-safe. Fix: added shared status codes, server-only `_error` logging, a status-page factory, focused coverage, and pushed `agent/custom-error-pages-main`.
- `2026-07-09` - `Custom error pages`: Common browser error states lacked branded recovery pages and direct error routes would be auth-redirected. Approach: add shared safe error UI, thin status pages, middleware public-route handling, and focused coverage. Fix: implemented 403/404/429/500/502/503/504 pages, `_error` fallback, route status handlers, middleware classification tests, and pushed `agent/custom-error-pages-main` for review.
- `2026-07-09` - `Retry-After additive jitter`: SERVICE_UNAVAILABLE retry jitter could shorten server Retry-After cooldowns and spend retry budget before the backend was ready. Approach: split server-guided jitter from client-generated backoff. Fix: used additive-only capped jitter for Retry-After delays and updated focused apiRequest fake-timer coverage.
- `2026-07-09` - `Chunk 5 parallel GET follow-up`: The merged Chunk 5 branch ended on gated summary-before-jobs behavior after a review nit was applied, so the latency goal was not implemented on main. Approach: restore `Promise.all()` only after storage repair, keep summary failure precedence, and keep jobs policy input on `storageStatusResult`. Fix: `handleGet()` now starts storage summary and job-list reads concurrently, discards jobs results when summary fails, and the focused route tests prove parallel start plus summary-error precedence.
- `2026-07-08` - `Chunk 6 latency merge conflict resolution`: The Chunk 6 PR conflicted with current main's mutation response work and docs logs. Approach: merge latest `origin/main`, preserve service-derived POST storage summaries, and align DELETE with main's id-only no-summary response. Fix: resolved route, test, and docs conflicts, kept create RPC count-hint summaries, removed the stale post-create/post-delete summary reads from the branch, and revalidated the focused suites.
- `2026-07-05` - `CSRF signature comparison byte-length guard`: The multibyte CSRF fix only covered the cookie/header comparison; the HMAC signature comparison in `validateCsrfToken` still guarded `timingSafeEqual` with UTF-16 string lengths, so a 64-character multibyte signature segment threw a RangeError and surfaced as a 503. Approach: mirror the already-reviewed buffer-based pattern at the remaining comparison and prove the crash path with an identical-token regression test. Fix: built UTF-8 buffers before the signature length guard and comparison, added the multibyte signature-segment test, and pushed the two-file commit to `codex/stripe-audit-followups`.
- `2026-07-05` - `Security review fixes main`: Review findings covered salary constraint rollout locking, update success-callback error handling, duplicated rate-limit 429 handling, and locked-delete branch coverage. Approach: verify each finding against current code, keep fixes at the smallest affected boundary, and add focused regression coverage. Fix: split salary CHECK validation into a follow-up migration, moved update callbacks outside the PUT failure path, reused the shared rate-limit exceeded helper, covered the settled locked-delete denial branch, and prepared `review/security-review-fixes-main` for review.
- `2026-07-01` - `Job create storage summary response`: Add/delete success flows could trigger an avoidable count-only storage-status read after POST create even though the server had already reconciled storage state. Approach: keep the legacy created-job array response while attaching optional count-only storage metadata and preserving the status-refresh fallback. Fix: POST `/api` now returns optional `storageSummary`, add success applies fresh mutation summaries without an extra read, stale mutation summaries fall back safely, and focused route/hook coverage verifies the contract.
- `2026-07-01` - `Job list pagination service validation`: Direct service callers could pass one-sided or malformed pagination options and fall back to an unpaginated job list read. Approach: keep the fix at the `getJobsByUserId()` service boundary with schema validation and the existing result-envelope contract. Fix: added `InvalidJobReadOptionsError` with 400 metadata, validated `from`/`to` before query construction, covered invalid ranges in the focused service suite, and pushed `review/job-data-performance-chunk-3` for review.
- `2026-06-30` - `Conditional job list counts`: Unpaginated dashboard list reads still requested an exact database count even though the full row set was already returned. Approach: keep Chunk 3 scoped to the list service contract, deriving full-list totals from returned rows while preserving exact totals for paginated reads. Fix: made `getJobsByUserId()` request exact counts only with `from`/`to`, added focused normal and locked archive coverage, and pushed `review/job-data-performance-chunk-3` for review.
- `2026-06-30` - `Job ordered-read index`: Dashboard active-list reads only had an owner/storage filter index and still needed sort work plus a deterministic tie-breaker for matching timestamps. Approach: keep the change scoped to Chunk 2 with a partial active-list index and service ordering update, leaving Premium and locked archive behavior unchanged. Fix: added `jobs_active_list_idx`, ordered job lists by `created_at DESC, id DESC`, covered the query shape in unit tests plus a gated migration catalog suite, and pushed `review/job-data-performance-chunk-2` for review.
- `2026-06-30` - `Storage count boundary hardening`: Review found the storage-count migration only checked for `public.jobs` and the service entrypoint let malformed user ids reach the admin RPC. Approach: keep fixes at the migration and exported service boundaries with fail-fast validation. Fix: added prerequisite checks for `public.jobs.user_id` and `public.jobs.storage_state`, added schema-backed `getJobStorageCounts()` user-id validation with a typed 400 error, and covered malformed ids in the focused service suite.
- `2026-06-25` - `Job storage count round trips`: Dashboard and storage-status loads paid three sequential count-only jobs queries for active, locked, and retained totals. Approach: keep the public summary contract unchanged while moving the count aggregation into one owner-scoped service-role RPC. Fix: added `get_job_storage_counts_for_user(p_user_id uuid)`, wired `getJobStorageCounts()` to one strict RPC call with malformed-payload fail-closed handling, and added focused unit plus gated integration coverage before pushing `job-data-performance`.
- `2026-06-25` - `Premium storage Chunk 11 H7 fixture id`: The final H7 restore tie-breaker fixture mixed explicit job ids with an active baseline row that relied on a DB default, causing Supabase bulk inserts to send a null id in the live integration run. Approach: keep the fix scoped to the integration fixture and preserve the deterministic tie-breaker assertions. Fix: gave the baseline row an explicit `randomUUID()` id and verified Suite H passes against the pre-production Supabase database before pushing.
- `2026-06-24` - `Premium storage Chunk 11 coverage hardening`: The final restore suite still asserted the stale 3-arg restore RPC, Suites F/G (premium restore, locked bulk delete) lacked the destructive opt-in gate, and concurrency/route/CSV edge cases were uncovered. Approach: verify each gap against the Chunk 11 worktree, mirror Suite H's destructive gate onto F/G, align Suite H with the 4-arg `restore_locked_jobs_for_premium_user(uuid,text,integer,text[])` signature, and add focused negative/race coverage. Fix: gated Suites F/G behind `RUN_DESTRUCTIVE_DB_INTEGRATION` + matching `SUPABASE_TEST_PROJECT_REF`, whitelisted the overflow/restore/bulk-delete suites in `.gitignore`, updated Suite H restore calls/privilege assertions and asserted the stale 3-arg overload is absent, added allowlist/stale-caller negatives (H4b), cross-operation races (H8), a concurrent bulk-delete (Suite G), strict locked-jobs route rejection, and hostile-CSV coverage; ran the non-destructive suites green and confirmed the gated DB suites skip cleanly before pushing.
- `2026-06-24` - `Destructive DB integration target gating`: DB integration suites could run against any Supabase project when common URL/key env vars were present. Approach: verify the live-DB gates against the Chunk 11 PR worktree and require both explicit destructive-test opt-in and a matching Supabase test project ref. Fix: gated the tracked destructive suites with `RUN_DESTRUCTIVE_DB_INTEGRATION`, `SUPABASE_TEST_PROJECT_REF`, and URL matching, then validated safe skips plus mismatched-target refusal before pushing `premium-storage-chunk-11`.
- `2026-06-22` - `Premium storage Chunk 11 integration evidence`: Chunk 11 needed final rollout evidence for edge cases not covered by the earlier per-chunk integration suites. Approach: branch from the clean Chunk 10 worktree, add one final-state integration suite, and avoid unrelated dirty workspace files. Fix: covered the complete storage migration stack, SQL storage-status matrix, service-role RPC boundaries, real locked-row projections, export visibility, and deterministic id tie-breakers, then pushed `premium-storage-chunk-11`.
- `2026-06-21` - `Premium storage Chunk 10 callback and RPC payload hardening`: Review found locked archive parent-refresh callback failures could be shown as delete failures and malformed successful RPC counts could normalize to zero. Approach: keep fixes at the locked archive UI/service boundaries with focused regressions. Fix: moved the parent callback outside delete-error handling, centralized delete cleanup, validated required successful RPC count fields, and covered both paths.
- `2026-06-21` - `Premium storage Chunk 10 bulk-delete hardening`: Review found locked archive bulk delete needed duplicate-submit protection, complete archive draining across bounded RPC batches, and sequential integration migrations. Approach: keep the fixes scoped to the locked archive UI, service, migration test setup, and focused regressions. Fix: added the confirm-click in-flight latch and thrown-error cleanup, looped bounded delete RPC calls until the archive drains or fails closed, included migration 019 before migration 020 in the DB evidence setup, and covered duplicate clicks plus multi-pass/fail-closed service behavior.
- `2026-06-21` - `Premium storage Chunk 10 review branch`: The locked archive needed a clean review branch with terminal-Free-only bulk deletion and no unrelated dirty workspace files. Approach: branch from `premium-storage-chunk-9`, apply only Chunk 10 files in a linked worktree, validate focused suites, and push `premium-storage-chunk-10`. Fix: added the bounded service-role delete RPC, confirmed API route, dedicated rate limit, second-confirmation archive UI, focused tests, and pushed the branch for review.
- `2026-06-21` - `Premium storage Chunk 9 fetch rejection and stale loading guard`: Code review found rejected full job fetches could bypass normalized error cleanup and stale full-fetch responses could clear loading while newer fetches were pending. Approach: keep the patch inside `useJobsQuery`, route thrown fetch errors through the existing normalized error path, and make stale full-fetch returns state-neutral. Fix: added try/catch around `/api` fetches, removed stale-branch loading cleanup, added focused hook regressions, and verified the hook suite before pushing.
- `2026-06-20` - `Premium storage Chunk 9 full-fetch loading guard`: Code review found stale full job fetches could leave dashboard loading stuck and shared freshness with count-only storage refreshes. Approach: split full-fetch and storage-refresh request refs, settle loading on stale full-fetch returns, and cover the refetch/refresh overlap. Fix: added `jobsRequestRef`, kept storage refreshes on `storageSummaryRequestRef`, added focused hook regression coverage, and verified the hook suite before pushing.
- `2026-06-20` - `Premium storage Chunk 9 stale fetch and archive coverage`: Code review found full job fetches could apply stale jobs/loading state, and terminal-Free archive copy lacked positive page coverage. Approach: verify both paths against the current Chunk 9 branch and keep the patch at the hook/page-test boundary. Fix: added a stale full-fetch early return, removed redundant post-freshness guards, covered the terminal-Free export notice, and verified focused suites before pushing the review branch update.
- `2026-06-20` - `Premium storage Chunk 9 review fixes`: Review found dashboard storage warnings/counts could go stale after add/delete mutations and billing could silently hide storage-status failures. Approach: keep active-row updates local, refresh count-only storage metadata after count-changing mutations, guard latest refresh responses, and surface storage-status partial failures without confirmed downgrade copy. Fix: added storage-summary refresh wiring, billing storage warning UI, focused hook/page regressions, and pushed the branch update.
- `2026-06-20` - `Storage export review hardening`: Code review found manual export query validation, weak CSV payload shape checks, and a generic invalid-user service error. Approach: verify each finding against the current Chunk 8 branch, keep the fixes at the route/service boundary, and add focused regression coverage. Fix: added strict empty Zod query validation, failed closed on missing or non-string CSV payloads, introduced `InvalidUserIdError` with code/status metadata, and verified the route and service suites before pushing the review branch update.
- `2026-06-18` - `Job CSV export pagination stability`: Large CSV exports could duplicate or skip jobs because offset pagination ordered only by `created_at`. Approach: replace offset paging with a deterministic keyset cursor while keeping `id` internal-only. Fix: selected `id` for cursoring, paged by `(created_at, id)` descending, failed closed on missing cursor fields, added focused regression coverage, and pushed the review branch update.
- `2026-06-18` - `Premium storage Chunk 8 review branch`: The locked-storage plan needed an explicit CSV export branch for CodeRabbit review without mixing in unrelated dirty work. Approach: base the review branch on current `origin/main`, keep export data owner-scoped, and limit CSV output to user-facing job fields. Fix: added `/api/storage/export`, dedicated `storage_export` rate limits of 5/hour and 25/day, CSV escaping/no-store download headers, focused route/service coverage, and pushed the clean Chunk 8 branch for review.
- `2026-06-18` - `Premium restore ordering fixture`: The restore-ordering integration assertion accidentally included its pre-existing active baseline row because the fixture company name used the same restored-row prefix. Approach: keep the fix scoped to the test fixture while preserving the RPC ordering assertion. Fix: renamed the active baseline seed row, verified the focused Premium restore integration suite, and prepared `review/premium-storage-chunk-7` for push.
- `2026-06-18` - `Premium storage Chunk 7 repair hardening`: Code review found storage-transition repair paths that handled returned error envelopes but not thrown rejections or malformed success envelopes. Approach: keep the fix in the shared transition boundary and thin route helpers, preserving the existing 503 fail-closed route flow. Fix: normalized downgrade/restore rejections, rejected missing dependency data, added route-level rejection guards, verified focused suites, and pushed `review/premium-storage-chunk-7`.
- `2026-06-18` - `Premium restore entitlement hardening`: Chunk 7 restore review found that the service-role restore boundary could trust active billing rows without the Premium price allowlist and could return stale Premium status after a canonical mismatch. Approach: keep the fix inside unapplied migration `019` and the Chunk 7 transition services, preserving fail-closed behavior. Fix: enforced DB-side Premium price allowlisting, passed configured allowlisted prices from the restore service, refreshed storage status after canonical non-Premium restore refusals, and added focused unit and integration regressions.
- `2026-06-18` - `Premium storage Chunk 7 review branch`: The Premium restore work needed a clean PR branch based on `main` for CodeRabbit review. Approach: create `review/premium-storage-chunk-7` from `origin/main`, stage only Chunk 7 restore files, and preserve unrelated dirty workspace changes. Fix: added service-role Premium restore RPC, shared storage-transition repair wiring, restore monitoring, focused regression coverage, and pushed the branch for review.
- `2026-06-14` - `Premium storage Chunk 6 review branch`: The downgrade overflow workflow needed concurrency hardening and a clean review branch based on Chunk 5. Approach: serialize billing and storage transitions, guard authoritative reconciliation with the observed billing snapshot, add lazy repair to job routes, and stage only Chunk 6 files from the dirty worktree. Fix: added idempotent terminal-Free overflow locking, canonical billing revalidation, create-versus-lock serialization, webhook and request-time repair, and focused unit and database race coverage.
- `2026-06-10` - `Premium chunk 4 review branch refresh`: The Chunk 4 CodeRabbit branch needed to be based on the latest `origin/main` while excluding unrelated dirty work from the prior branch. Approach: stash the dirty tree, create a fresh review branch from latest main, resolve the storage-create conflicts, and stage only the atomic create quota files. Fix: pushed `review/premium-storage-chunk-4` with the atomic create migration, service/API wiring, focused unit tests, and DB race integration coverage.
- `2026-06-10` - `Premium chunk docs conflict resolution`: The review branch and latest `main` both added top-of-file entries to `docs/feature-memory.md` and `docs/fixes.md`, causing merge conflicts. Approach: merge latest `origin/main` into the review branch and preserve both branches' notes in descending date order. Fix: kept the June 8 storage/job entries, added main's June 7 sync-pending entries beneath them, and verified the focused job and billing service suites.
- `2026-06-08` - `Job create server-field sanitization`: `createJob` could persist server-controlled job fields from caller-supplied payloads when using the admin insert path. Approach: reuse the existing server-field stripping helper before insert and keep the server-derived `user_id` assignment authoritative. Fix: sanitized create payloads before `.insert()`, added focused regression coverage for malicious `id`, `user_id`, `storage_state`, and `locked_*` fields, and verified the job service suite.
- `2026-06-08` - `Premium downgrade storage boundary`: The paid-to-free storage degradation work needed a database boundary so authenticated owners could not bypass locked-row policy through direct `jobs` table access. Approach: add storage-state schema fields, constraints, indexes, and move ordinary job CRUD behind server-owned service-role access with owner filters. Fix: added migration `016`, direct-access integration evidence, service-layer storage-field stripping, focused job service tests, and pushed the clean Chunk 2 branch for review.
- `2026-06-07` - `Sync-pending storage retry mismatch`: Sync-pending create flow was marked retryable while the top-level storage retryability helper correctly excluded it, producing inconsistent response flags. Approach: preserve the Chunk 1 plan contract that only billing-unavailable and reconciliation-pending storage states are retryable. Fix: set the sync-pending create-flow retry flag to false, updated the focused billing-service expectation, and verified the suite.
- `2026-05-31` - `Stripe webhook RPC ambiguity`: Real Stripe webhook events reached the local app but failed while syncing subscriptions because the event-driven billing upsert RPC used PL/pgSQL variable names that collided with CTE result columns. Approach: ship a forward database migration that preserves the RPC contract while removing the ambiguous names, and make the migration harness detect already-installed broken RPC bodies. Fix: added migration `015`, wired the billing migration integration harness to apply and assert it, and documented the webhook reconciliation impact.
- `2026-05-28` - `Billing service test env restore`: The billing service test suite set `STRIPE_PRICE_PREMIUM_MONTHLY` in `beforeEach` without restoring the prior process environment. Approach: capture the original value once and restore or delete it after each test. Fix: added `STRIPE_PRICE_PREMIUM_MONTHLY` cleanup to the suite lifecycle and verified the focused billing service tests.
- `2026-05-28` - `Stripe item period selection`: Multi-item Stripe subscription events could derive the item-level `current_period_end` from the first incoming item instead of the locally tracked premium price. Approach: prefer the persisted local price only when it still appears in the Stripe item snapshot. Fix: threaded the preferred price through event/delete billing sync helpers and added multi-item regression coverage.
- `2026-05-26` - `Stripe Chunk 7 broad PR cleanup`: The Chunk 7 operational readiness branch was based on an old main commit, which made the PR show a broad 104-file diff and risked stale conflict resolution. Approach: merge latest `origin/main`, resolve conflicts at the Stripe billing boundaries, and carry forward only the reviewed Chunk 7 plus bug-fix layer. Fix: preserved the Chunk 7 plan/code, restored checkout/portal/webhook/rate-limit fixes from the backup stash, excluded local agent artifacts, and prepared the narrowed branch for focused verification.
- `2026-05-20` - `Stripe webhook dispatcher payload and timestamp validation`: Subscription-delete webhooks could pass string payloads into the delete helper, and digit-only string `created` values logged as invalid dates instead of Stripe epoch seconds. Approach: keep the checks at the dispatcher boundary and cover both reviewed paths with focused tests. Fix: require delete payloads to be non-array objects before extracting subscription ids, normalize digit-only `created` strings with `Number(created) * 1000`, record feature-memory notes, and verify the dispatcher suite in-band.
- `2026-05-20` - `Stripe webhook dispatcher audit findings`: Malformed subscription-delete webhooks lacked the dispatcher malformed-event code, future timestamp pre-read failures had no dedicated signal, receipt mismatch logs mixed timestamp formats, and delete dispatcher coverage was thin. Approach: keep fixes in the dispatcher boundary, preserve fail-closed retries, and add focused tests for the reviewed paths. Fix: validated deleted subscription ids before calling the billing service, logged `billing_webhook_future_timestamp_rejected`, normalized mismatch log timestamps to ISO, added delete-path dispatcher coverage, and verified the focused billing/webhook suites.
- `2026-05-20` - `Billing receipt timestamp simplification`: The receipt merge RPC used a tautological `processed_at` CASE after validating `p_result` to an allowed result value. Approach: verify the current migration first and keep the SQL change to the update assignment only. Fix: replaced the unreachable CASE and fallback with direct `processed_at = pg_catalog.now()` in migration `011`.
- `2026-05-19` - `Stripe Chunk 6 webhook and receipt hardening`: Chunk 6 needed the public webhook implementation plus follow-up fixes for checkout email validation, receipt envelope mismatch leakage, and trackable SQL integration coverage. Approach: keep the patch at the billing boundaries, preserve fail-closed behavior, and add regression tests around each confirmed issue. Fix: added the webhook dispatcher/route coverage, tightened authenticated checkout email gating, sanitized mismatch SQL/service errors, unignored the billing migration integration suite, documented the `exec_sql` setup requirement, and verified the focused billing plus route-safety suites.
- `2026-05-17` - `Markdown lint cleanup and Stripe env restore`: Docs had still-valid markdownlint spacing/title/list-style findings, and one Stripe runtime test could leave `process.env.NODE_ENV` mutated after its production-only reset assertion. Approach: verify each finding against current files, keep the markdown edits mechanical, and scope the test change to the mutating assertion. Fix: added the missing patches title, normalized ordered-list prefixes, padded the focused-test code fence, added week-heading blank lines in feature memory, and restored `NODE_ENV` in a `finally` block around `runtime.__resetForTests()`.
- `2026-05-17` - `Pending checkout review hardening`: Pending checkout migration and service helpers had still-valid review gaps around service-role grants, unsupported plan rows, owner-scoped finalize/fail writes, and stale hour-bucket docs. Approach: verify each finding against current code, keep schema/service changes minimal, and align docs with the current `billing_checkout_sessions` model. Fix: granted service-role table/sequence access in migration `013`, added plan allowlist checks, required user ids for finalize/fail writes, threaded authenticated owners through checkout cleanup/finalize, guarded the Stripe test reset helper, refreshed focused coverage, and reframed docs around pending-session dedupe.
- `2026-05-11` - `Stripe runtime custom-env cache isolation`: Custom Stripe env snapshots could populate the global runtime config cache, and same-day idempotency notes could be read as leaving the old hour-bucket model authoritative. Approach: keep caching limited to `process.env` resolutions and clarify the historical checkout idempotency sequence in docs. Fix: `resolveStripeConfig(env)` now validates arbitrary env objects without writing cached config or cached errors unless `env === process.env`, regression tests cover custom success and failure snapshots, and the fix/feature notes now state that nonce-backed checkout idempotency supersedes the hour-bucket model.
- `2026-05-11` - `Checkout idempotency, webhook env selection, and auth log severity`: Checkout retries could replay stale hour-bucket Stripe sessions, webhook secret lookup ignored explicit env snapshots after Stripe mode caching, and expected 401/403 token failures were logged as errors. Approach: verify each finding against current code and keep the patches at the failing boundary. Fix: restored validated per-attempt checkout nonces in the client/schema/server idempotency key, made webhook secret selection derive mode from a provided `STRIPE_SECRET_KEY` before falling back to cached mode, lowered auth-invalid token validation logs to warnings, and added focused regression coverage.
- `2026-05-11` - `Stripe billing foundation hardening`: Billing local reads and webhook verification were coupled to checkout-only Stripe config, webhook raw-body caps were inconsistent, Supabase auth outages collapsed into 401s, client checkout nonces allowed duplicate Checkout Sessions, and completed checkout reconciliation could hide non-entitled local state. Approach: split narrow Stripe runtime helpers from checkout config, enforce raw-body byte caps at the actual read path, classify auth provider outages, initially move checkout idempotency to a server-owned UTC hour-bucket idempotency model (historical; superseded by the nonce-backed restore in the `2026-05-11` `Checkout idempotency, webhook env selection, and auth log severity` entry), make completed non-entitled reconciliation terminal, and fix webhook verifier config failures to return service unavailable. Fix: added `stripeRuntime.js`, rewired billing/webhook Stripe calls, threaded `maxBodyBytes`, returned retryable `503` auth and verifier failures, made checkout bodies strict `{ plan }` for that intermediate model, refreshed checkout-status terminal-state handling, updated docs, and added focused regression coverage. Current checkout request contract honors validated `checkoutAttemptNonce` client nonces after schema validation; the nonce-backed server-built Stripe idempotency key is authoritative, and the server-owned UTC hour-bucket idempotency model is no longer used.
- `2026-05-09` - `Billing success duplicate refresh and review guidance`: The billing success page manual refresh button could start duplicate checkout-status polls on rapid repeated clicks before React disabled the control, and the repo guidance did not explicitly require automated reviewers to verify findings against current code. Approach: add a minimal in-flight latch around the manual refresh flow, cover the race with a focused page test, and add repo-level review-quality guidance that emphasizes current-code verification and concrete trigger paths. Fix: `success.js` now guards manual refresh with `refreshPending` and clears it when the active refresh attempt settles, `success.test.js` proves duplicate clicks are ignored, and `AGENTS.md` plus `CLAUDE.md` now require actionable, non-speculative review findings.
- `2026-05-09` - `Billing success rejected-poll recovery`: The billing success page could leave the UI in its continuing poll state if a checkout-status poll promise rejected before the result interpreter ran. Approach: verify the current page-level polling flow, add a local fail-closed catch at the poll boundary, and cover the rejected-poll branch with a focused page test. Fix: `success.js` now normalizes successful poll responses inside the guarded block, clears timers and cooldown on rejected polls, settles the page into the terminal error state, documents the `interpreted` state-machine flow inline, and includes a regression test that proves polling stops after a rejected follow-up poll.
- `2026-05-06` - `Billing auth/action and rate-limit validation fixes`: Public-route IP extraction still accepted malformed address-shaped strings, the billing page treated real `401` status reads as outages, billing checkout/portal clicks could race into duplicate POSTs before React re-rendered, and the Stripe runtime test suite restored an unset `NODE_ENV` as the literal `"undefined"` string. Approach: verify each reported finding against current code, keep the production-path changes narrowly scoped, preserve existing fail-closed and redirect-helper behavior, and add focused regression coverage where the bug lived. Fix: `withRateLimit.js` now uses strict `node:net` `isIP()` checks with added malformed-header tests, the billing page now signs out and redirects to `/login` on unauthorized status loads, checkout/portal handlers now use an immediate ref-backed in-flight latch plus optimistic loading state to block duplicate submissions, and `stripe.test.js` now deletes `process.env.NODE_ENV` during teardown when it was originally unset.
- `2026-05-06` - `Billing Stage 4 polling and checkout hardening`: Billing checkout retries could reuse stale Checkout Session URLs, success-page polling could overrun the billing-write quota or ignore server cooldowns, checkout-status retried too broadly, and mapped Stripe-customer email syncs were doing avoidable no-op provider updates. Approach: tighten the shared billing contracts at the schema, route, and client-helper layers; make the Stripe runtime and checkout-status retry behavior explicit; add a best-effort local email fingerprint optimization; and bundle the Stage 4 billing routes/pages with the new hardening changes on one branch. Fix: raised the `billing_write` quotas, replaced hour-bucket checkout idempotency with a validated client nonce, collapsed valid-session ownership failures, added explicit Stripe timeout/retry config and app-origin credential rejection, extended `apiRequest` with response metadata for `Retry-After`, added cooldown-aware success-page polling/refresh helpers, kept billing redirect loading latched until handoff, added the `012_billing_customer_email_fingerprint.sql` migration plus fingerprint-backed Stripe email dedupe, and refreshed the focused Jest coverage and docs.
- `2026-04-30` - `Billing customer email sync hardening`: Stripe customer warning logs could include full provider error objects, the email sync helper lacked module-style documentation, and a post-upsert mapped-customer race path skipped the best-effort email sync. Approach: tighten the logging payload to safe primitives, document the helper's side effects and return behavior, and align the race branch with the other mapped-customer email-sync paths while adding regression coverage. Fix: `billingService.js` now logs a sanitized `{ name, code, message }` error shape, documents `syncStripeCustomerEmail` with repo-style JSDoc, and syncs email before returning from the post-upsert mapped-customer branch; the billing service Jest suite now covers the race-path email sync and the sanitized warning payload.
- `2026-04-30` - `Fix log created`: Established the shared post-push fix log. Approach: created a lightweight markdown tracker. Fix: added a standard place to record issues, approaches, and resolutions after each push.
- `2026-04-30` - `Billing hardening narrowed follow-up`: Stripe customer creation could deadlock on retries when the idempotent create payload changed with email input, and delete-event billing writes could overwrite newer local state by merging stale fields. Approach: keep the Stripe customer create call byte-stable, move email sync into a separate best-effort update, and treat delete snapshots as event-derived writes guarded by small future-timestamp validation. Fix: `billingService.js` now creates customers with a stable empty payload and syncs email separately, delete events no longer merge local subscription fields into the RPC payload, future event timestamps beyond five minutes are rejected, stale outcomes use the write enum consistently, and the billing unit suite covers the new behavior.
